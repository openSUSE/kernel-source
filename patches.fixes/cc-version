--- linux-2.6.8/Makefile.~1~	2004-08-30 17:41:50.072602248 +0200
+++ linux-2.6.8/Makefile	2004-08-30 18:06:28.813918703 +0200
@@ -279,8 +279,8 @@ cc-option-yn = $(shell if $(CC) $(CFLAGS
 
 # cc-version
 # Usage gcc-ver := $(call cc-version $(CC))
-cc-version = $(CONFIG_SHELL) $(srctree)/scripts/gcc-version.sh \
-              $(if $(1), $(1), $(CC))
+cc-version = $(shell $(CONFIG_SHELL) $(srctree)/scripts/gcc-version.sh \
+			$(if $(1), $(1), $(CC)))
 
 
 # Look for make include files relative to root of kernel src
