ChangeSet
  1.1587.19.4 04/05/03 23:02:10 sri@us.ibm.com +2 -0
  [SCTP] Fix memset() parameter ordering.

  net/sctp/ulpqueue.c
    1.31 04/05/03 23:01:42 sri@us.ibm.com +1 -1
    [SCTP] Fix memset() parameter ordering.

  net/sctp/ulpevent.c
    1.27 04/05/03 23:01:42 sri@us.ibm.com +1 -1
    [SCTP] Fix memset() parameter ordering.

diff -Nru a/net/sctp/ulpevent.c b/net/sctp/ulpevent.c
--- a/net/sctp/ulpevent.c	Sun May  9 06:17:40 2004
+++ b/net/sctp/ulpevent.c	Sun May  9 06:17:40 2004
@@ -77,7 +77,7 @@
 /* Initialize an ULP event from an given skb.  */
 void sctp_ulpevent_init(struct sctp_ulpevent *event, int msg_flags)
 {
-	memset(event, sizeof(struct sctp_ulpevent), 0x00);
+	memset(event, 0, sizeof(struct sctp_ulpevent));
 	event->msg_flags = msg_flags;
 }
 
diff -Nru a/net/sctp/ulpqueue.c b/net/sctp/ulpqueue.c
--- a/net/sctp/ulpqueue.c	Sun May  9 06:17:40 2004
+++ b/net/sctp/ulpqueue.c	Sun May  9 06:17:40 2004
@@ -79,7 +79,7 @@
 struct sctp_ulpq *sctp_ulpq_init(struct sctp_ulpq *ulpq,
 				 struct sctp_association *asoc)
 {
-	memset(ulpq, sizeof(struct sctp_ulpq), 0x00);
+	memset(ulpq, 0, sizeof(struct sctp_ulpq));
 
 	ulpq->asoc = asoc;
 	skb_queue_head_init(&ulpq->reasm);
.........................................................................
# vim: syntax=diff

