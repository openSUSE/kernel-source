diff -u linux/arch/i386/kernel/dmi_scan.c-o linux/arch/i386/kernel/dmi_scan.c
--- linux/arch/i386/kernel/dmi_scan.c-o	2004-05-28 12:30:52.000000000 +0200
+++ linux/arch/i386/kernel/dmi_scan.c	2004-06-04 01:20:56.000000000 +0200
@@ -583,8 +583,14 @@
 #ifdef	CONFIG_ACPI_PCI
 static __init int disable_acpi_pci(struct dmi_blacklist *d) 
 { 
-	printk(KERN_NOTICE "%s detected: force use of pci=noacpi\n", d->ident); 	
-	acpi_noirq_set();
+	if (!acpi_force) { 
+		printk(KERN_NOTICE "%s detected: force use of pci=noacpi\n", d->ident); 	
+		acpi_noirq_set();
+	} else { 
+		printk(KERN_NOTICE 
+		       "Warning: acpi=force overrules DMI blacklist for %s\n",
+		       d->ident); 
+	} 
 	return 0;
 } 
 
