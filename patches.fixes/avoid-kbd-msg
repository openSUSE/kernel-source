From: ak@suse.de
Subject: Remove most useless printk in the world

Index: linux-2.6.15/drivers/input/keyboard/atkbd.c
===================================================================
--- linux-2.6.15.orig/drivers/input/keyboard/atkbd.c
+++ linux-2.6.15/drivers/input/keyboard/atkbd.c
@@ -339,7 +339,6 @@ static irqreturn_t atkbd_interrupt(struc
 			atkbd_report_key(atkbd->dev, regs, KEY_HANJA, 3);
 			goto out;
 		case ATKBD_RET_ERR:
-			printk(KERN_DEBUG "atkbd.c: Keyboard on %s reports too many keys pressed.\n", serio->phys);
 			goto out;
 	}
 
@@ -363,6 +362,7 @@ static irqreturn_t atkbd_interrupt(struc
 				       "like XFree86, might be trying access hardware directly.\n",
 				       data == ATKBD_RET_ACK ? "ACK" : "NAK", serio->phys);
 			} else {
+#if 0
 				printk(KERN_WARNING "atkbd.c: Unknown key %s "
 				       "(%s set %d, code %#x on %s).\n",
 				       atkbd->release ? "released" : "pressed",
@@ -371,6 +371,7 @@ static irqreturn_t atkbd_interrupt(struc
 				printk(KERN_WARNING "atkbd.c: Use 'setkeycodes %s%02x <keycode>' "
 				       "to make it known.\n",
 				       code & 0x80 ? "e0" : "", code & 0x7f);
+#endif
 			}
 			input_sync(atkbd->dev);
 			break;
