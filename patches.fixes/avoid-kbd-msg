From: ak@suse.de
Subject: Remove most useless printk in the world

Index: linux-2.6.13/drivers/input/keyboard/atkbd.c
===================================================================
--- linux-2.6.13.orig/drivers/input/keyboard/atkbd.c
+++ linux-2.6.13/drivers/input/keyboard/atkbd.c
@@ -328,7 +328,6 @@ static irqreturn_t atkbd_interrupt(struc
 			atkbd_report_key(&atkbd->dev, regs, KEY_HANJA, 3);
 			goto out;
 		case ATKBD_RET_ERR:
-			printk(KERN_DEBUG "atkbd.c: Keyboard on %s reports too many keys pressed.\n", serio->phys);
 			goto out;
 	}
 
