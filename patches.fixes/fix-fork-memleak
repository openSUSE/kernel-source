diff -urNp linux-2.6.4/kernel/fork.c linux-2.6.4.SUSE/kernel/fork.c
--- linux-2.6.4/kernel/fork.c	2004-04-16 14:29:17.000000000 +0200
+++ linux-2.6.4.SUSE/kernel/fork.c	2004-04-16 14:30:32.000000000 +0200
@@ -1103,6 +1103,8 @@ bad_fork_cleanup_namespace:
 	exit_namespace(p);
 bad_fork_cleanup_mm:
 	exit_mm(p);
+	if (p->active_mm)
+		mmdrop(p->active_mm);
 bad_fork_cleanup_signal:
 	exit_signal(p);
 bad_fork_cleanup_sighand:
