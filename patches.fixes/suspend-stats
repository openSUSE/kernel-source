Index: linux-2.6.8/net/core/rtnetlink.c
===================================================================
--- linux-2.6.8.orig/net/core/rtnetlink.c
+++ linux-2.6.8/net/core/rtnetlink.c
@@ -217,9 +217,11 @@ static int rtnetlink_fill_ifinfo(struct 
 		u32 master = dev->master->ifindex;
 		RTA_PUT(skb, IFLA_MASTER, sizeof(master), &master);
 	}
 
-	if (dev->get_stats) {
+	/* Don't call get_stats when the device is in low power state */
+	if (dev->get_stats &&
+	    (!dev->class_dev.dev || !dev->class_dev.dev->power_state)) { 
 		unsigned long *stats = (unsigned long*)dev->get_stats(dev);
 		if (stats) {
 			struct rtattr  *a;
 			__u32	       *s;
