diff -u linux/net/core/rtnetlink.c-o linux/net/core/rtnetlink.c
--- linux/net/core/rtnetlink.c-o	2004-06-11 16:33:02.000000000 +0200
+++ linux/net/core/rtnetlink.c	2004-06-12 23:36:42.000000000 +0200
@@ -192,7 +192,9 @@
 			dev->qdisc_sleeping->ops->id);
 	if (dev->master)
 		RTA_PUT(skb, IFLA_MASTER, sizeof(int), &dev->master->ifindex);
-	if (dev->get_stats) {
+	/* Don't call get_stats when the device is in low power state */
+	if (dev->get_stats && 
+	    (!dev->class_dev.dev || !dev->class_dev.dev->power_state)) { 
 		unsigned long *stats = (unsigned long*)dev->get_stats(dev);
 		if (stats) {
 			struct rtattr  *a;
