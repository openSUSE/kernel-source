Security issue, not yet disclosed. Could allow local root.

diff -urNp linux-2.6.4/drivers/video/fbmem.c linux-2.6.4.SUSE/drivers/video/fbmem.c
--- linux-2.6.4/drivers/video/fbmem.c	2004-04-16 14:29:10.000000000 +0200
+++ linux-2.6.4.SUSE/drivers/video/fbmem.c	2004-04-16 14:32:40.000000000 +0200
@@ -1038,7 +1038,7 @@ fb_ioctl(struct inode *inode, struct fil
 	case FBIOGETCMAP:
 		if (copy_from_user(&cmap, (void *) arg, sizeof(cmap)))
 			return -EFAULT;
-		return (fb_copy_cmap(&info->cmap, &cmap, 0));
+		return (fb_copy_cmap(&info->cmap, &cmap, 2));
 	case FBIOPAN_DISPLAY:
 		if (copy_from_user(&var, (void *) arg, sizeof(var)))
 			return -EFAULT;
