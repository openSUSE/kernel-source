diff -urNp linux-2.6.4/fs/read_write.c linux-2.6.4.SUSE/fs/read_write.c
--- linux-2.6.4/fs/read_write.c	2004-03-17 15:01:36.000000000 +0100
+++ linux-2.6.4.SUSE/fs/read_write.c	2004-03-17 15:02:54.000000000 +0100
@@ -418,7 +418,7 @@ static ssize_t do_readv_writev(int type,
 		if (len < 0)	/* size_t not fitting an ssize_t .. */
 			goto out;
 		tot_len += len;
-		if (tot_len < tmp) /* maths overflow on the ssize_t */
+		if ((ssize_t)tot_len < tmp) /* maths overflow on the ssize_t */
 			goto out;
 	}
 	if (tot_len == 0) {
