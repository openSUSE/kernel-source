diff -urNp linux-2.6.5/sound/core/timer.c linux-2.6.5.SUSE/sound/core/timer.c
--- linux-2.6.5/sound/core/timer.c	2004-06-16 17:30:36.381804897 +0200
+++ linux-2.6.5.SUSE/sound/core/timer.c	2004-06-16 17:33:39.444110492 +0200
@@ -1437,12 +1437,12 @@ static int snd_timer_user_gstatus(struct
 		err = -ENODEV;
 	}
 	up(&register_mutex);
-	if (err >= 0 && copy_from_user(_gstatus, &gstatus, sizeof(gstatus)))
+	if (err >= 0 && copy_to_user(_gstatus, &gstatus, sizeof(gstatus)))
 		err = -EFAULT;
 	return err;
 }
 
-static int snd_timer_user_tselect(struct file *file, snd_timer_select_t *_tselect)
+static int snd_timer_user_tselect(struct file *file, snd_timer_select_t __user *_tselect)
 {
 	snd_timer_user_t *tu;
 	snd_timer_select_t tselect;
