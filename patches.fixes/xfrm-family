--- linux-2.6.5.patched/net/xfrm/xfrm_user.c	2004-04-04 05:37:23.000000000 +0200
+++ linux-2.6.5/net/xfrm/xfrm_user.c	2004-04-20 10:51:04.260728312 +0200
@@ -679,6 +679,7 @@ static int copy_to_user_tmpl(struct xfrm
 		up->reqid = kp->reqid;
 		up->mode = kp->mode;
 		up->share = kp->share;
+		up->family = xp->family;
 		up->optional = kp->optional;
 		up->aalgos = kp->aalgos;
 		up->ealgos = kp->ealgos;
