Index: linux-2.6.5/arch/i386/boot/Makefile
===================================================================
--- linux-2.6.5.orig/arch/i386/boot/Makefile
+++ linux-2.6.5/arch/i386/boot/Makefile
@@ -102,4 +102,3 @@ zlilo: $(BOOTIMAGE)
 
 install: $(BOOTIMAGE)
 	sh $(srctree)/$(src)/install.sh $(KERNELRELEASE) $< System.map "$(INSTALL_PATH)"
-	if [ -f init/kerntypes.o ]; then cp init/kerntypes.o $(INSTALL_PATH)/Kerntypes; fi
Index: linux-2.6.5/arch/ppc64/boot/Makefile
===================================================================
--- linux-2.6.5.orig/arch/ppc64/boot/Makefile
+++ linux-2.6.5/arch/ppc64/boot/Makefile
@@ -124,6 +124,5 @@ $(obj)/imagesize.c: vmlinux
 
 install: $(CONFIGURE) $(obj)/$(BOOTIMAGE)
 	sh -x $(src)/install.sh "$(KERNELRELEASE)" "$(obj)/$(BOOTIMAGE)" "$(TOPDIR)/System.map" "$(INSTALL_PATH)"
-	if [ -f init/kerntypes.o ]; then cp init/kerntypes.o $(INSTALL_PATH)/Kerntypes; fi
 
 clean-files := $(patsubst $(obj)/%,%, $(obj-boot))
Index: linux-2.6.5/arch/s390/boot/install.sh
===================================================================
--- linux-2.6.5.orig/arch/s390/boot/install.sh
+++ linux-2.6.5/arch/s390/boot/install.sh
@@ -35,14 +35,5 @@ if [ -f $5/System.map ]; then
 	mv $5/System.map $5/System.old
 fi
 
-if [ -f $5/Kerntypes ]; then
-	mv $5/Kerntypes $5/Kerntypes.old
-fi
-
 cat $2 > $5/vmlinuz
 cp $3 $5/System.map
-
-# copy the kernel type file if it exists
-if [ -f $4 ]; then
-	cp $4 $5/Kerntypes
-fi
Index: linux-2.6.5/arch/x86_64/boot/Makefile
===================================================================
--- linux-2.6.5.orig/arch/x86_64/boot/Makefile
+++ linux-2.6.5/arch/x86_64/boot/Makefile
@@ -99,4 +99,3 @@ zlilo: $(BOOTIMAGE)
 
 install: $(BOOTIMAGE)
 	sh $(srctree)/$(src)/install.sh $(KERNELRELEASE) $(BOOTIMAGE) System.map "$(INSTALL_PATH)"
-	if [ -f init/kerntypes.o ]; then cp init/kerntypes.o $(INSTALL_PATH)/Kerntypes; fi
