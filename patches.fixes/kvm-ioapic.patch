From: agraf@suse.de
Subject: Ignore apic polarity
Patch-mainline: unknown
References: bnc#556564

Index: linux-2.6.31-openSUSE-11.2/virt/kvm/ioapic.c
===================================================================
--- linux-2.6.31-openSUSE-11.2.orig/virt/kvm/ioapic.c
+++ linux-2.6.31-openSUSE-11.2/virt/kvm/ioapic.c
@@ -179,7 +179,8 @@ int kvm_ioapic_set_irq(struct kvm_ioapic
 
 	if (irq >= 0 && irq < IOAPIC_NUM_PINS) {
 		entry = ioapic->redirtbl[irq];
-		level ^= entry.fields.polarity;
+// polarity is always active high in qemu
+//		level ^= entry.fields.polarity;
 		if (!level)
 			ioapic->irr &= ~mask;
 		else {
