Index: linux-2.6.5/drivers/net/pcnet32.c
===================================================================
--- linux-2.6.5.orig/drivers/net/pcnet32.c	2004-06-01 09:54:47.000000000 +0200
+++ linux-2.6.5/drivers/net/pcnet32.c	2004-06-01 10:03:19.000000000 +0200
@@ -1885,6 +1885,11 @@
 	    } else {
 		int rx_in_place = 0;
 
+		if (unlikely(pkt_len > PKT_BUF_SZ - 2)) {
+		    if (netif_msg_drv(lp))
+		        printk(KERN_ERR "%s: Impossible packet size %u!\n", dev->name, pkt_len);
+		    skb = NULL;
+	        } else
 		if (pkt_len > rx_copybreak) {
 		    struct sk_buff *newskb;
 
