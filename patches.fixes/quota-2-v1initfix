This patch fixes problem when quota information was read after end of
file (which can happen when user has sufficiently large UID and does not
have any files yet).

Index: linux-2.6.5/fs/quota_v1.c
===================================================================
--- linux-2.6.5.orig/fs/quota_v1.c
+++ linux-2.6.5/fs/quota_v1.c
@@ -52,6 +52,8 @@ static int v1_read_dqblk(struct dquot *d
 
 	/* Now we are sure filp is valid */
 	offset = v1_dqoff(dquot->dq_id);
+	/* Set structure to 0s in case read fails/is after end of file */
+	memset(&dqblk, 0, sizeof(struct v1_disk_dqblk));
 	fs = get_fs();
 	set_fs(KERNEL_DS);
 	filp->f_op->read(filp, (char *)&dqblk, sizeof(struct v1_disk_dqblk), &offset);
