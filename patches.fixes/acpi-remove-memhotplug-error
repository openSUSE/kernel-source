From: ak@suse.de
References: SUSE152732
Subject: Remove annoying message from acpi memory hotplug code

Is very noisy on many machines and doesn't serve a useful
purpose as far as I can see.

Index: linux-2.6.15/drivers/acpi/acpi_memhotplug.c
===================================================================
--- linux-2.6.15.orig/drivers/acpi/acpi_memhotplug.c
+++ linux-2.6.15/drivers/acpi/acpi_memhotplug.c
@@ -455,7 +455,6 @@ acpi_memory_register_notify_handler(acpi
 
 	status = is_memory_device(handle);
 	if (ACPI_FAILURE(status)){
-		ACPI_EXCEPTION((AE_INFO, status, "handle is no memory device"));
 		return_ACPI_STATUS(AE_OK);	/* continue */
 	}
 
@@ -475,7 +474,6 @@ acpi_memory_deregister_notify_handler(ac
 
 	status = is_memory_device(handle);
 	if (ACPI_FAILURE(status)){
-		ACPI_EXCEPTION((AE_INFO, status, "handle is no memory device"));
 		return_ACPI_STATUS(AE_OK);	/* continue */
 	}
 
