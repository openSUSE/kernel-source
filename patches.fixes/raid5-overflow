diff -u linux/drivers/md/raid5.c-o linux/drivers/md/raid5.c
--- linux/drivers/md/raid5.c-o	2004-04-05 21:49:07.000000000 +0200
+++ linux/drivers/md/raid5.c	2004-04-07 13:14:20.000000000 +0200
@@ -1334,7 +1334,7 @@
 		disk_stat_add(mddev->gendisk, read_sectors, bio_sectors(bi));
 	}
 
-	logical_sector = bi->bi_sector & ~(STRIPE_SECTORS-1);
+	logical_sector = bi->bi_sector & ~((sector_t)STRIPE_SECTORS-1);
 	last_sector = bi->bi_sector + (bi->bi_size>>9);
 
 	bi->bi_next = NULL;
