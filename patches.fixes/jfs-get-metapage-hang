diff -urp linux-2.6.5-20040525/fs/jfs/jfs_metapage.c linux/fs/jfs/jfs_metapage.c
--- linux-2.6.5-20040525/fs/jfs/jfs_metapage.c	2004-05-26 11:45:49.000000000 -0500
+++ linux/fs/jfs/jfs_metapage.c	2004-05-26 14:14:29.000000000 -0500
@@ -239,6 +239,7 @@ again:
 		spin_unlock(&meta_lock);
 		if (test_bit(META_stale, &mp->flag)) {
 			release_metapage(mp);
+			yield();	/* Let other waiters release it, too */
 			goto again;
 		}
 		if (test_bit(META_discard, &mp->flag)) {
