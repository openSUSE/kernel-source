From:  	 Jay Lan <jlan@sgi.com>
Subject: export some symbols for SGI GPL kernel modules
Patch-mainline: No
References: SUSE46082, 141533

export some symbols for SGI GPL kernel modules

Acked-by: garloff@suse.de
Acked-by: rw@suse.de

Index: linux-2.6.14/kernel/fork.c
===================================================================
--- linux-2.6.14.orig/kernel/fork.c
+++ linux-2.6.14/kernel/fork.c
@@ -122,6 +122,7 @@
 	if (!profile_handoff_task(tsk))
 		free_task(tsk);
 }
+EXPORT_SYMBOL_GPL(__put_task_struct);
 
 void __init fork_init(unsigned long mempages)
 {
Index: linux-2.6.14/arch/ia64/kernel/setup.c
===================================================================
--- linux-2.6.14.orig/arch/ia64/kernel/setup.c
+++ linux-2.6.14/arch/ia64/kernel/setup.c
@@ -74,6 +74,7 @@
 DEFINE_PER_CPU(unsigned long, ia64_phys_stacked_size_p8);
 unsigned long ia64_cycles_per_usec;
 struct ia64_boot_param *ia64_boot_param;
+EXPORT_SYMBOL_GPL(ia64_boot_param);
 struct screen_info screen_info;
 unsigned long vga_console_iobase;
 unsigned long vga_console_membase;
Index: linux-2.6.14/mm/memory.c
===================================================================
--- linux-2.6.14.orig/mm/memory.c
+++ linux-2.6.14/mm/memory.c
@@ -871,6 +871,7 @@
 		tlb_finish_mmu(tlb, address, end);
 	return end;
 }
+EXPORT_SYMBOL_GPL(zap_page_range);
 
 /*
  * Do a quick page-table lookup for a single page.
