Subject: [ALSA] Improve the sequencer client name
From: Takashi Iwai <tiwai@suse.de>

Improved the sequencer client name of external MIDI devices.

--- linux-2.6.8/sound/core/seq/seq_midi.c	2004-09-29 19:27:10.558131686 +0200
+++ linux/sound/core/seq/seq_midi.c	2004-09-22 13:10:35.000000000 +0200
@@ -279,7 +279,7 @@ static int set_client_name(seq_midisynth
 	cinfo.client = client->seq_client;
 	cinfo.type = KERNEL_CLIENT;
 	name = rmidi->name[0] ? (const char *)rmidi->name : "External MIDI";
-	snprintf(cinfo.name, sizeof(cinfo.name), "%s - Rawmidi %d", name, card->number);
+	strlcpy(cinfo.name, name, sizeof(cinfo.name));
 	return snd_seq_kernel_client_ctl(client->seq_client, SNDRV_SEQ_IOCTL_SET_CLIENT_INFO, &cinfo);
 }
 
