diff -urNp linux-2.6.3/kernel/module.c linux-2.6.3.SUSE/kernel/module.c
--- linux-2.6.3/kernel/module.c	2004-02-27 16:12:16.000000000 +0100
+++ linux-2.6.3.SUSE/kernel/module.c	2004-02-27 16:13:21.000000000 +0100
@@ -749,7 +749,9 @@ sys_delete_module(const char __user *nam
 		wait_for_zero_refcount(mod);
 
 	/* Final destruction now noone is using it. */
+	up(&module_mutex);
 	mod->exit();
+	down(&module_mutex);
 	free_module(mod);
 
  out:
