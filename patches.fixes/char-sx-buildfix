--- linux-2.6.0-test7/drivers/char/sx.c.sx	2003-10-09 11:35:43.000000000 +0200
+++ linux-2.6.0-test7/drivers/char/sx.c	2003-10-09 11:35:58.000000000 +0200
@@ -1419,8 +1419,10 @@
 	}
 
 	line = tty->index;
+#if 0
 	sx_dprintk (SX_DEBUG_OPEN, "%d: opening line %d. tty=%p ctty=%p, np=%d)\n", 
 	            current->pid, line, tty, current->tty, sx_nports);
+#endif
 
 	if ((line < 0) || (line >= SX_NPORTS) || (line >= sx_nports))
 		return -ENODEV;
