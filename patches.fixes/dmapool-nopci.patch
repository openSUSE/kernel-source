dmapool is currently used only by pci, so do not compile it for
non-pci systems. Is contained in 2.6.3-rc1-mm1, so can be removed once
it is merged from there.

			hare 2004/02/12

Index: linux-2.6.7/drivers/base/Makefile
===================================================================
--- linux-2.6.7.orig/drivers/base/Makefile	2004-06-16 07:19:52.000000000 +0200
+++ linux-2.6.7/drivers/base/Makefile	2004-08-04 11:30:44.200332942 +0200
@@ -2,7 +2,8 @@
 
 obj-y			:= core.o sys.o interface.o bus.o \
 			   driver.o class.o class_simple.o platform.o \
-			   cpu.o firmware.o init.o map.o dmapool.o
+			   cpu.o firmware.o init.o map.o
+obj-$(CONFIG_PCI)	+= dmapool.o
 obj-y			+= power/
 obj-$(CONFIG_FW_LOADER)	+= firmware_class.o
 obj-$(CONFIG_NUMA)	+= node.o
