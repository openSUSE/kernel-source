Index: linux-2.6.5/fs/jfs/jfs_txnmgr.c
===================================================================
--- linux-2.6.5.orig/fs/jfs/jfs_txnmgr.c
+++ linux-2.6.5/fs/jfs/jfs_txnmgr.c
@@ -256,8 +256,8 @@ int txInit(void)
 	 * transaction id (tid) = tblock index
 	 * tid = 0 is reserved.
 	 */
-	TxLockLWM = nTxLock * .4;
-	TxLockHWM = nTxLock * .8;
+	TxLockLWM = nTxLock * 2 / 5;
+	TxLockHWM = nTxLock * 4 / 5;
 
 	size = sizeof(struct tblock) * nTxBlock;
 	TxBlock = (struct tblock *) vmalloc(size);
