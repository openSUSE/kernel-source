--- linux-2.6.5/fs/bio.c~	2004-09-13 15:27:10.042767690 +0200
+++ linux-2.6.5/fs/bio.c	2004-09-13 15:27:27.877856442 +0200
@@ -600,18 +600,6 @@
 	int i;
 
 	/*
-	 * find original bio if it was bounced
-	 */
-	if (bio->bi_private) {
-		/*
-		 * someone stole our bio, must not happen
-		 */
-		BUG_ON(!bio_flagged(bio, BIO_BOUNCED));
-	
-		bio = bio->bi_private;
-	}
-
-	/*
 	 * make sure we dirty pages we wrote to
 	 */
 	__bio_for_each_segment(bvec, bio, i, 0) {
