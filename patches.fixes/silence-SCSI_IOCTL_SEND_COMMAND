--- linux-2.6.8/drivers/block/scsi_ioctl.c~	2004-09-29 15:13:22.804560369 +0200
+++ linux-2.6.8/drivers/block/scsi_ioctl.c	2004-09-29 15:13:34.762274850 +0200
@@ -533,7 +533,6 @@
 		 * old junk scsi send command ioctl
 		 */
 		case SCSI_IOCTL_SEND_COMMAND:
-			printk(KERN_WARNING "program %s is using a deprecated SCSI ioctl, please convert it to SG_IO\n", current->comm);
 			err = -EINVAL;
 			if (!arg)
 				break;
