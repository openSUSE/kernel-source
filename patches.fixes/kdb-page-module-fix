Date: Mon, 16 Jan 2006 14:55:49 +1100
From: Nathan Scott <nathans@sgi.com>
Subject: KDB page module build fix

Fix compile error from KDB module builds resulting from the merge
up to a new kernel.

Acked-by: Andreas Gruenbacher <agruen@suse.de>

Index: linux/kdb/modules/kdbm_pg.c
===================================================================
--- linux.orig/kdb/modules/kdbm_pg.c
+++ linux/kdb/modules/kdbm_pg.c
@@ -228,7 +228,7 @@
 	if (page_has_buffers(&page))
 		kdb_printf("  buffers 0x%p\n", page_buffers(&page));
 	else
-		kdb_printf("  private 0x%lx\n", page.u.private);
+		kdb_printf("  private 0x%lx\n", page_private(&page));
 
 	return 0;
 }
