Patch from Chuck Lever at NetApp

The O_DIRECT patch introduced in 2.4 had some code to select the blocksize
based on the server's advertised wtmult value. This causes all sorts of
problems with large disks, and can also degrade performance.

And with 2.6, it's not necessary anymore, so we just go back to using
the specified wsize as s_blocksize

Index: linux-2.6.7/fs/nfs/inode.c
===================================================================
--- linux-2.6.7.orig/fs/nfs/inode.c	2004-08-04 14:13:42.387707822 +0200
diff -urNp linux-2.6.8/fs/nfs/inode.c linux-2.6.8.SUSE/fs/nfs/inode.c
--- linux-2.6.8/fs/nfs/inode.c	2004-08-30 15:23:24.430238816 +0200
+++ linux-2.6.8.SUSE/fs/nfs/inode.c	2004-08-30 15:23:43.147240895 +0200
@@ -296,6 +296,10 @@ nfs_sb_init(struct super_block *sb, rpc_
 							 &sb->s_blocksize_bits);
 	server->wtmult = nfs_block_bits(fsinfo.wtmult, NULL);
 
+	if (sb->s_blocksize == 0)
+		sb->s_blocksize = nfs_block_bits(server->wsize,
+							 &sb->s_blocksize_bits);
+
 	server->dtsize = nfs_block_size(fsinfo.dtpref, NULL);
 	if (server->dtsize > PAGE_CACHE_SIZE)
 		server->dtsize = PAGE_CACHE_SIZE;
