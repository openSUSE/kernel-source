diff -urp linux-2.6.5-7.61/fs/jfs/jfs_metapage.c linux/fs/jfs/jfs_metapage.c
--- linux-2.6.5-7.61/fs/jfs/jfs_metapage.c	2004-05-30 15:54:32.000000000 -0500
+++ linux/fs/jfs/jfs_metapage.c	2004-05-30 18:02:40.799091816 -0500
@@ -225,8 +225,16 @@ struct metapage *__get_metapage(struct i
 
 	if (absolute)
 		mapping = inode->i_sb->s_bdev->bd_inode->i_mapping;
-	else
+	else {
+		/*
+		 * If an nfs client tries to read an inode that is larger
+		 * than any existing inodes, we may try to read past the
+		 * end of the inode map
+		 */
+		if ((lblock << inode->i_blkbits) >= inode->i_size)
+			return NULL;
 		mapping = inode->i_mapping;
+	}
 
 	hash_ptr = meta_hash(mapping, lblock);
 again:
