diff -ur linux-2.6.8-5.org/drivers/input/mouse/psmouse-base.c linux-2.6.8-5/drivers/input/mouse/psmouse-base.c
--- linux-2.6.8-5.org/drivers/input/mouse/psmouse-base.c	2004-08-14 07:36:32.000000000 +0200
+++ linux-2.6.8-5/drivers/input/mouse/psmouse-base.c	2004-08-26 23:29:21.571644768 +0200
@@ -178,6 +178,20 @@
 		printk(KERN_WARNING "psmouse.c: %s at %s lost synchronization, throwing %d bytes away.\n",
 		       psmouse->name, psmouse->phys, psmouse->pktcnt);
 		psmouse->pktcnt = 0;
+		psmouse->error++;
+		/*
+		 * this maybe come from a internal resetted mouse e.g. a IMPS2 mouse
+		 * which falled back into PS2 mode, so if resetafter parameter is set
+		 * try a reconnect
+		 */
+		if (psmouse_resetafter && (psmouse->error >= psmouse_resetafter)) {
+			psmouse->error = 0;
+			psmouse->state = PSMOUSE_IGNORE;
+			printk(KERN_NOTICE "psmouse.c: %s at %s issuing reconnect request\n",
+				psmouse->name, psmouse->phys);
+			serio_reconnect(psmouse->serio);
+			goto out;
+		}
 	}
 
 	psmouse->last = jiffies;
diff -ur linux-2.6.8-5.org/drivers/input/mouse/psmouse.h linux-2.6.8-5/drivers/input/mouse/psmouse.h
--- linux-2.6.8-5.org/drivers/input/mouse/psmouse.h	2004-08-14 07:37:40.000000000 +0200
+++ linux-2.6.8-5/drivers/input/mouse/psmouse.h	2004-08-26 23:16:12.453608944 +0200
@@ -56,7 +56,7 @@
 	unsigned char state;
 	char acking;
 	volatile char ack;
-	char error;
+	unsigned char error;
 	char devname[64];
 	char phys[32];
 
