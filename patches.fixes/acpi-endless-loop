From: trenn@suse.de
Subject: [ACPI] Fix ACPI device scan
References: 116763

Some HP notebooks would run into an endless loop scanning the ACPI device
list. This patch fixes it.

--- vanilla-linux-2.6.14-rc3.orig/drivers/acpi/scan.c	2005-10-03 18:21:35.000000000 +0200
+++ vanilla-linux-2.6.14-rc3/drivers/acpi/scan.c	2005-10-03 18:21:58.000000000 +0200
@@ -1111,7 +1111,7 @@
 	 *
 	 * TBD: Assumes LDM provides driver hot-plug capability.
 	 */
-	result = acpi_bus_find_driver(device);
+	acpi_bus_find_driver(device);
 
       end:
 	if (!result)
