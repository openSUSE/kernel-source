Subject: [ALSA] Fix RME32 Oops
From: Takashi Iwai <tiwai@suse.de>

Fix the oops on rme32 driver with half-duplex mode.

--- linux-2.6.8/sound/pci/rme32.c	2004-09-29 19:27:45.197287237 +0200
+++ linux/sound/pci/rme32.c	2004-09-20 12:08:35.000000000 +0200
@@ -1123,7 +1123,7 @@ snd_rme32_pcm_trigger(snd_pcm_substream_
 	}
 	
 	/* prefill playback buffer */
-	if (cmd == SNDRV_PCM_TRIGGER_START) {
+	if (cmd == SNDRV_PCM_TRIGGER_START && rme32->fullduplex_mode) {
 		snd_pcm_group_for_each(pos, substream) {
 			s = snd_pcm_group_substream_entry(pos);
 			if (s == rme32->playback_substream) {
