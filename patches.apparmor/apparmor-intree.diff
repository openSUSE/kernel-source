From: John Johansen <jjohansen@suse.de>
Subject: Add AppArmor LSM to security/Makefile

Signed-off-by: John Johansen <jjohansen@suse.de>
Signed-off-by: Andreas Gruenbacher <agruen@suse.de>

---
 security/Kconfig          |    1 +
 security/Makefile         |    1 +
 security/apparmor/Kconfig |    3 ++-
 3 files changed, 4 insertions(+), 1 deletion(-)

--- a/security/Kconfig	2007-10-31 17:42:53.000000000 -0400
+++ b/security/Kconfig	2007-10-31 17:45:25.000000000 -0400
@@ -94,6 +94,7 @@ config SECURITY_ROOTPLUG
 	  If you are unsure how to answer this question, answer N.
 
 source security/selinux/Kconfig
+source security/apparmor/Kconfig
 
 endmenu
 
--- a/security/Makefile	2007-10-31 17:45:11.000000000 -0400
+++ b/security/Makefile	2007-10-31 17:45:25.000000000 -0400
@@ -12,5 +12,6 @@ obj-y		+= commoncap.o
 obj-$(CONFIG_SECURITY)			+= security.o dummy.o inode.o
 # Must precede capability.o in order to stack properly.
 obj-$(CONFIG_SECURITY_SELINUX)		+= selinux/built-in.o
+obj-$(CONFIG_SECURITY_APPARMOR)		+= commoncap.o apparmor/
 obj-$(CONFIG_SECURITY_CAPABILITIES)	+= capability.o
 obj-$(CONFIG_SECURITY_ROOTPLUG)		+= root_plug.o
--- a/security/apparmor/Kconfig	2007-10-31 17:45:24.000000000 -0400
+++ b/security/apparmor/Kconfig	2007-10-31 17:45:25.000000000 -0400
@@ -1,6 +1,7 @@
 config SECURITY_APPARMOR
 	tristate "AppArmor support"
-	depends on SECURITY!=n
+	depends on SECURITY
+	select AUDIT
 	help
 	  This enables the AppArmor security module.
 	  Required userspace tools (if they are not included in your
