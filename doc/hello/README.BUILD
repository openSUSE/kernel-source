BUILDING THIS DEMO MODULE:

 * Install the kernel-source.$ARCH.rpm package.

 * Choose for which kernel flavor the module shall be built in Makefile.
   The default is to build for the currently running kernel.

 * Compile the module with:

    make

 * Install the module with:

    make install
