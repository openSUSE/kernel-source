Index: linux-2.6.8/arch/um/drivers/net_kern.c
===================================================================
--- linux-2.6.8.orig/arch/um/drivers/net_kern.c	2004-08-20 10:58:01.071451176 +0200
+++ linux-2.6.8/arch/um/drivers/net_kern.c	2004-08-20 10:58:26.823254864 +0200
@@ -129,6 +129,7 @@ static int uml_net_open(struct net_devic
 	spin_lock(&opened_lock);
 	list_add(&lp->list, &opened);
 	spin_unlock(&opened_lock);
+	while((err = uml_net_rx(dev)) > 0) ; /* clear buffer */
  out:
 	spin_unlock(&lp->lock);
 	return(err);
