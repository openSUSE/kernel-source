Index: uml-2.6.9-rc2/arch/um/Makefile
===================================================================
--- uml-2.6.9-rc2.orig/arch/um/Makefile	2004-09-16 16:37:08.937335485 +0200
+++ uml-2.6.9-rc2/arch/um/Makefile	2004-09-16 16:44:39.246194209 +0200
@@ -46,11 +46,11 @@ MAKEFILE-$(CONFIG_MODE_TT) += Makefile-t
 MAKEFILE-$(CONFIG_MODE_SKAS) += Makefile-skas
 
 ifneq ($(MAKEFILE-y),)
-  include $(addprefix $(ARCH_DIR)/,$(MAKEFILE-y))
+  include $(addprefix $(srctree)/$(ARCH_DIR)/,$(MAKEFILE-y))
 endif
 
-include $(ARCH_DIR)/Makefile-$(SUBARCH)
-include $(ARCH_DIR)/Makefile-os-$(OS)
+include $(srctree)/$(ARCH_DIR)/Makefile-$(SUBARCH)
+include $(srctree)/$(ARCH_DIR)/Makefile-os-$(OS)
 
 #EXTRAVERSION := $(EXTRAVERSION)-1um
 
