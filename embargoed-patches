# List of embargoed patches and the end of the embargoes. Automatic CVS
# builds are built excluding the embargoed patches (using
# scripts/tar-up.sh --embargo), so that automatically built kernels will
# not leak embargoed patches by accident.
#
# Format:
# `date --rfc-822`  patch-name

