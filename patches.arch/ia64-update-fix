Index: linux-2.6.0/fs/proc/task_mmu.c
===================================================================
--- linux-2.6.0.orig/fs/proc/task_mmu.c	2004-01-05 15:36:49.383374473 +0100
+++ linux-2.6.0/fs/proc/task_mmu.c	2004-01-05 16:09:22.113728106 +0100
@@ -123,9 +123,11 @@ static int show_map(struct seq_file *m, 
 		if (len < 1)
 			len = 1;
 		seq_printf(m, "%*c", len, ' ');
+#ifdef AT_SYSINFO_EHDR
 		if (map == gate_map())
 			seq_printf (m, "%s", gate_dso_path);
 		else
+#endif
 			seq_path(m, file->f_vfsmnt, file->f_dentry, " \t\n\\");
 	}
 	seq_putc(m, '\n');
