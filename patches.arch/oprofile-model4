Subject: blablabla
From: ak@suse.de

CVS Commit checks sucks.

diff -up linux/arch/i386/oprofile/nmi_int.c-o linux/arch/i386/oprofile/nmi_int.c
--- linux/arch/i386/oprofile/nmi_int.c-o	2004-05-10 09:43:22.000000000 +0200
+++ linux/arch/i386/oprofile/nmi_int.c	2005-01-27 16:18:42.000000000 +0100
@@ -313,11 +313,12 @@ static int __init p4_init(void)
 {
 	__u8 cpu_model = current_cpu_data.x86_model;
 
-	if (cpu_model > 3)
+	nmi_ops.cpu_type = "i386/p4"; /* or different type for timer? */
+
+	if (cpu_model > 4)
 		return 0;
 
 #ifndef CONFIG_SMP
-	nmi_ops.cpu_type = "i386/p4";
 	model = &op_p4_spec;
 	return 1;
 #else
