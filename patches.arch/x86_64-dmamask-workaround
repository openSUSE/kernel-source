diff -u linux/arch/x86_64/kernel/pci-gart.c-o linux/arch/x86_64/kernel/pci-gart.c
--- linux/arch/x86_64/kernel/pci-gart.c-o	2004-03-17 04:20:50.000000000 +0100
+++ linux/arch/x86_64/kernel/pci-gart.c	2004-03-17 06:33:09.000000000 +0100
@@ -195,6 +195,10 @@
 	   uses the normal dma_mask for alloc_consistent. */
 	dma_mask &= hwdev->dma_mask;
 
+#if 1 /* workaround for bug for now */
+	dma_mask &= 0xffffffff;
+#endif
+
 	memory = (void *)__get_free_pages(gfp, get_order(size));
 	if (memory == NULL) {
 		return NULL; 
