Fix oops when multiple MCE entries are logged.

Index: linux/arch/x86_64/kernel/mce.c
===================================================================
--- linux.orig/arch/x86_64/kernel/mce.c	2004-09-27 22:01:44.%N +0200
+++ linux/arch/x86_64/kernel/mce.c	2004-09-28 12:01:09.%N +0200
@@ -101,7 +101,7 @@
 		if (time_before(tsc, start))
 			continue;
 		print_mce(&mcelog.entry[i]); 
-		if (mcelog.entry[i].tsc == backup->tsc)
+		if (backup && mcelog.entry[i].tsc == backup->tsc)
 			backup = NULL;
 	}
 	if (backup)
