Subject:  Quieten down microcode update driver
From: ak@suse.de

Only log data in microcode driver when something is changed
Otherwise it was far too noisy on large systems.

Index: linux-2.6.15/arch/i386/kernel/microcode.c
===================================================================
--- linux-2.6.15.orig/arch/i386/kernel/microcode.c
+++ linux-2.6.15/arch/i386/kernel/microcode.c
@@ -201,8 +201,6 @@ static inline void mark_microcode_update
 	} else if (mc_header->rev == uci->rev) {
 		/* notify the caller of success on this cpu */
 		uci->err = MC_SUCCESS;
-		printk(KERN_ERR "microcode: CPU%d already at revision"
-			" 0x%x (current=0x%x)\n", cpu_num, mc_header->rev, uci->rev);
 		goto out;
 	}
 
@@ -366,7 +364,6 @@ static void do_update_one (void * unused
 	struct ucode_cpu_info *uci = ucode_cpu_info + cpu_num;
 
 	if (uci->mc == NULL) {
-		printk(KERN_INFO "microcode: No new microcode data for CPU%d\n", cpu_num);
 		return;
 	}
 
