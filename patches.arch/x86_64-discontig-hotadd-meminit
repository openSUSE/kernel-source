From: ak@suse.de
Suse-bugzilla: 142035
Subject: Fix build with discontigmem hotadd

Index: linux-2.6.15/include/linux/init.h
===================================================================
--- linux-2.6.15.orig/include/linux/init.h
+++ linux-2.6.15/include/linux/init.h
@@ -241,7 +241,7 @@ void __init parse_early_param(void);
 #define __cpuexitdata	__exitdata
 #endif
 
-#ifdef CONFIG_MEMORY_HOTPLUG
+#if defined(CONFIG_MEMORY_HOTPLUG) || defined(CONFIG_ACPI_HOTPLUG_MEMORY)
 #define __meminit
 #define __meminitdata
 #define __memexit
