--- linux-2.6.6-bk/arch/x86_64/kernel/setup.c~	2004-05-27 17:51:39.000000000 -0700
+++ linux-2.6.6-bk/arch/x86_64/kernel/setup.c	2004-06-01 13:32:45.000000000 -0700
@@ -253,6 +253,7 @@
 		/* acpi=ht just means: do ACPI MADT parsing 
 		   at bootup, but don't enable the full ACPI interpreter */
 		if (!memcmp(from, "acpi=ht", 7)) { 
+			disable_acpi();
 			acpi_ht = 1; 
 		}
                 else if (!memcmp(from, "pci=noacpi", 10)) 
@@ -535,8 +536,7 @@
         * the bootmem allocator) but before get_smp_config (to allow parsing
         * of MADT).
         */
-	if (!acpi_disabled)
-		acpi_boot_init();
+	acpi_boot_init();
 #endif
 #ifdef CONFIG_X86_LOCAL_APIC
 	/*
