--- linux-2.6.5rc2-amd64/arch/x86_64/Makefile-o	2004-03-21 21:12:10.000000000 +0100
+++ linux-2.6.5rc2-amd64/arch/x86_64/Makefile	2004-03-25 06:57:06.000000000 +0100
@@ -38,7 +38,7 @@
 LDFLAGS_vmlinux := -e stext
 
 cflags-$(CONFIG_MK8) += $(call check_gcc,-march=k8,)
-cflags-$(CONFIG_MPSC) += $(call check_gcc,-march=prescott,)
+cflags-$(CONFIG_MPSC) += $(call check_gcc,-march=nocona,)
 CFLAGS += $(cflags-y)
 
 CFLAGS += -mno-red-zone
