diff -u linux/arch/i386/kernel/setup.c-MPX linux/arch/i386/kernel/setup.c
--- linux/arch/i386/kernel/setup.c-MPX	2004-03-03 14:31:52.000000000 +0100
+++ linux/arch/i386/kernel/setup.c	2004-03-11 17:47:51.000000000 +0100
@@ -827,6 +827,13 @@
 	 */
 	reserve_bootmem(0, PAGE_SIZE);
 
+    /* could be an AMD 768MPX chipset. Reserve a page  before VGA to prevent
+       PCI prefetch (errata #56). Usually the page is reserved anyways,
+       unless you have no mouse plugged in. */
+	if (boot_cpu_data.x86_vendor == X86_VENDOR_AMD && 
+	    boot_cpu_data.x86 == 6) 
+	     reserve_bootmem(0xa0000 - 4096, 4096); 
+
 #ifdef CONFIG_SMP
 	/*
 	 * But first pinch a few for the stack/trampoline stuff
