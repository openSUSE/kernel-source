diff -u linux-2.6.5/arch/x86_64/kernel/smpboot.c-o linux-2.6.5/arch/x86_64/kernel/smpboot.c
--- linux-2.6.5/arch/x86_64/kernel/smpboot.c-o	2004-07-09 10:22:30.000000000 +0000
+++ linux-2.6.5/arch/x86_64/kernel/smpboot.c	2004-07-09 10:27:50.884131106 +0000
@@ -659,7 +659,7 @@
 			Dprintk("CPU has booted.\n");
 		} else {
 			boot_error = 1;
-			if (*((volatile unsigned char *)phys_to_virt(8192))
+			if (*((volatile unsigned char *)phys_to_virt(SMP_TRAMPOLINE_BASE))
 					== 0xA5)
 				/* trampoline started but...? */
 				printk("Stuck ??\n");
@@ -676,9 +676,6 @@
 		clear_bit(cpu, &cpu_initialized); /* was set by cpu_init() */
 		cpucount--;
 	}
-
-	/* mark "stuck" area as not stuck */
-	*((volatile unsigned *)phys_to_virt(8192)) = 0;
 }
 
 cycles_t cacheflush_time;
