Index: linux/drivers/net/tg3.c
===================================================================
--- linux.orig/drivers/net/tg3.c	2004-06-14 10:07:21.000000000 -0500
+++ linux/drivers/net/tg3.c	2004-06-14 10:15:43.000000000 -0500
@@ -2032,7 +2032,11 @@ static void tg3_tx(struct tg3 *tp)
 		int i;
 
 		if (unlikely(skb == NULL))
+#ifdef CONFIG_IA64_SGI_SN2
+			break;
+#else
 			BUG();
+#endif
 
 		pci_unmap_single(tp->pdev,
 				 pci_unmap_addr(ri, mapping),
