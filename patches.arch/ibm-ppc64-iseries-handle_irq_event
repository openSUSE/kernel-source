--- ./arch/ppc64/kernel/irq.c~	2004-04-29 19:27:09.000000000 +0200
+++ ./arch/ppc64/kernel/irq.c	2004-04-29 19:28:35.000000000 +0200
@@ -376,10 +376,8 @@ static inline int handle_irq_event(int i
 	int status = 0;
 	int retval = 0;
 
-#ifndef CONFIG_PPC_ISERIES
 	if (!(action->flags & SA_INTERRUPT))
 		local_irq_enable();
-#endif
 
 	do {
 		status |= action->flags;
@@ -388,9 +386,7 @@ static inline int handle_irq_event(int i
 	} while (action);
 	if (status & SA_SAMPLE_RANDOM)
 		add_interrupt_randomness(irq);
-#ifndef CONFIG_PPC_ISERIES
 	local_irq_disable();
-#endif
 	return retval;
 }
 
