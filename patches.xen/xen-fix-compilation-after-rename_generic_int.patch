From: Rafael J. Wysocki <rjw@suse.de>
Subject: XEN: Fix compilation after renaming of generic_irqs

Patch bug-561946_rename_generic_int.patch renamed generic_irqs
to x86_platform_ipis and broke xen compilation.  Fix that.

Signed-off-by: Rafael J. Wysocki <rjw@suse.de>
---
 arch/x86/kernel/irq-xen.c |    4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

Index: linux-2.6.32-master/arch/x86/kernel/irq-xen.c
===================================================================
--- linux-2.6.32-master.orig/arch/x86/kernel/irq-xen.c
+++ linux-2.6.32-master/arch/x86/kernel/irq-xen.c
@@ -79,7 +79,7 @@ static int show_other_interrupts(struct 
 	if (generic_interrupt_extension) {
 		seq_printf(p, "%*s: ", prec, "PLT");
 		for_each_online_cpu(j)
-			seq_printf(p, "%10u ", irq_stats(j)->generic_irqs);
+			seq_printf(p, "%10u ", irq_stats(j)->x86_platform_ipis);
 		seq_printf(p, "  Platform interrupts\n");
 	}
 #ifdef CONFIG_SMP
@@ -196,7 +196,7 @@ u64 arch_irq_stat_cpu(unsigned int cpu)
 	sum += irq_stats(cpu)->apic_pending_irqs;
 #endif
 	if (generic_interrupt_extension)
-		sum += irq_stats(cpu)->generic_irqs;
+		sum += irq_stats(cpu)->x86_platform_ipis;
 #ifdef CONFIG_SMP
 	sum += irq_stats(cpu)->irq_resched_count;
 	sum += irq_stats(cpu)->irq_call_count;
