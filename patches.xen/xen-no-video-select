From: jbeulich@novell.com
Subject: Suppress VIDEO_SELECT config option

The Xen guest doesn't know how to change video modes at boot.

Index: head-2006-02-16/drivers/video/Kconfig
===================================================================
--- head-2006-02-16.orig/drivers/video/Kconfig	2006-02-16 16:55:47.000000000 +0100
+++ head-2006-02-16/drivers/video/Kconfig	2006-02-17 12:44:03.000000000 +0100
@@ -495,7 +495,7 @@ config FB_HGA_ACCEL
 
 config VIDEO_SELECT
 	bool
-	depends on (FB = y) && X86
+	depends on (FB = y) && X86 && !X86_XEN && !X86_64_XEN
 	default y
 
 config FB_SGIVW
