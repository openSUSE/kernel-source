Subject: Separate out clearing of NMI I/O check error
From: http://xenbits.xensource.com/xen-unstable.hg (tip 13385)
Patch-mainline: obsolete
Acked-by: jbeulich@novell.com

Index: head-2007-01-16/arch/i386/kernel/traps.c
===================================================================
--- head-2007-01-16.orig/arch/i386/kernel/traps.c	2007-01-16 15:24:21.000000000 +0100
+++ head-2007-01-16/arch/i386/kernel/traps.c	2007-01-16 17:33:49.000000000 +0100
@@ -655,18 +655,11 @@ mem_parity_error(unsigned char reason, s
 static __kprobes void
 io_check_error(unsigned char reason, struct pt_regs * regs)
 {
-	unsigned long i;
-
 	printk(KERN_EMERG "NMI: IOCK error (debug interrupt?)\n");
 	show_registers(regs);
 
 	/* Re-enable the IOCK line, wait for a few seconds */
-	reason = (reason & 0xf) | 8;
-	outb(reason, 0x61);
-	i = 2000;
-	while (--i) udelay(1000);
-	reason &= ~8;
-	outb(reason, 0x61);
+	clear_io_check_error(reason);
 }
 
 static __kprobes void
Index: head-2007-01-16/include/asm-i386/mach-default/mach_traps.h
===================================================================
--- head-2007-01-16.orig/include/asm-i386/mach-default/mach_traps.h	2006-11-29 22:57:37.000000000 +0100
+++ head-2007-01-16/include/asm-i386/mach-default/mach_traps.h	2007-01-16 17:33:49.000000000 +0100
@@ -15,6 +15,18 @@ static inline void clear_mem_error(unsig
 	outb(reason, 0x61);
 }
 
+static inline void clear_io_check_error(unsigned char reason)
+{
+	unsigned long i;
+
+	reason = (reason & 0xf) | 8;
+	outb(reason, 0x61);
+	i = 2000;
+	while (--i) udelay(1000);
+	reason &= ~8;
+	outb(reason, 0x61);
+}
+
 static inline unsigned char get_nmi_reason(void)
 {
 	return inb(0x61);
