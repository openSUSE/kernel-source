From: jbeulich@novell.com
Subject: Don't restrict physical addresses to 36 bits.
Patch-mainline: obsolete

32on64 supports 168Gb for 32-bit guests, hence at least 38 bits must be
specified here. The foreign frame bit is MFN bit 31, so the maximum
valid value here would be 43.

Index: head-2007-01-16/include/asm-i386/mach-xen/asm/page.h
===================================================================
--- head-2007-01-16.orig/include/asm-i386/mach-xen/asm/page.h	2007-01-22 13:26:10.000000000 +0100
+++ head-2007-01-16/include/asm-i386/mach-xen/asm/page.h	2007-01-22 13:26:10.000000000 +0100
@@ -7,7 +7,7 @@
 #define PAGE_MASK	(~(PAGE_SIZE-1))
 
 #ifdef CONFIG_X86_PAE
-#define __PHYSICAL_MASK_SHIFT	36
+#define __PHYSICAL_MASK_SHIFT	40
 #define __PHYSICAL_MASK		((1ULL << __PHYSICAL_MASK_SHIFT) - 1)
 #define PHYSICAL_PAGE_MASK	(~((1ULL << PAGE_SHIFT) - 1) & __PHYSICAL_MASK)
 #else
