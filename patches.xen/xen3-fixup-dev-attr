Subject: adjust device attribute show functions
From: Jan Beulich <jbeulich@novell.com>
Suse-bugzilla: 140453

Account for 2.6.13-added parameter.

Signed-off-by: Jan Beulich <jbeulich@novell.com>

Index: head-2006-01-16/drivers/xen/blkback/xenbus.c
===================================================================
--- head-2006-01-16.orig/drivers/xen/blkback/xenbus.c	2006-01-16 10:07:48.534768048 +0100
+++ head-2006-01-16/drivers/xen/blkback/xenbus.c	2006-01-16 10:23:38.100412128 +0100
@@ -60,7 +60,8 @@ void update_blkif_status(blkif_t *blkif)
 }
 
 
-static ssize_t show_physical_device(struct device *_dev, char *buf)
+static ssize_t show_physical_device(struct device *_dev,
+                                    struct device_attribute *attr, char *buf)
 {
 	struct xenbus_device *dev = to_xenbus_device(_dev);
 	struct backend_info *be = dev->data;
@@ -70,7 +71,8 @@ DEVICE_ATTR(physical_device, S_IRUSR | S
 	    show_physical_device, NULL);
 
 
-static ssize_t show_mode(struct device *_dev, char *buf)
+static ssize_t show_mode(struct device *_dev,
+                        struct device_attribute *attr, char *buf)
 {
 	struct xenbus_device *dev = to_xenbus_device(_dev);
 	struct backend_info *be = dev->data;
