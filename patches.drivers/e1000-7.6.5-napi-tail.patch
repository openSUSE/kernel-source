From: Karsten Keil <kkeil@novell.com>
Subject: skb->tail is not longer always a pointer

Fix the e1000 7.6.5 version to always use skb_tail_pointer().

---
 drivers/net/e1000/e1000_main.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

--- a/drivers/net/e1000/e1000_main.c	2007-10-31 17:44:29.000000000 -0400
+++ b/drivers/net/e1000/e1000_main.c	2007-10-31 17:44:31.000000000 -0400
@@ -5286,7 +5286,7 @@ static boolean_t e1000_clean_rx_irq_ps(s
 				PCI_DMA_FROMDEVICE);
 			vaddr = kmap_atomic(ps_page->ps_page[0],
 			                    KM_SKB_DATA_SOFTIRQ);
-			memcpy(skb->tail, vaddr, l1);
+			memcpy(skb_tail_pointer(skb), vaddr, l1);
 			kunmap_atomic(vaddr, KM_SKB_DATA_SOFTIRQ);
 			pci_dma_sync_single_for_device(pdev,
 				ps_page_dma->ps_page_dma[0],
