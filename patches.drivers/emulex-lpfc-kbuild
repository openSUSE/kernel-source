diff -p -purNX /suse/olh/kernel/kernel_exclude.txt linux-2.6.3.emulex/drivers/scsi/Kconfig linux-2.6.3/drivers/scsi/Kconfig
--- linux-2.6.3.emulex/drivers/scsi/Kconfig	2004-03-01 13:16:05.000000000 +0100
+++ linux-2.6.3/drivers/scsi/Kconfig	2004-03-01 14:38:35.000000000 +0100
@@ -976,6 +976,14 @@ config SCSI_SYM53C8XX_IOMAPPED
 	  the card.  This is significantly slower then using memory
 	  mapped IO.  Most people should answer N.
 
+config SCSI_LPFCDD
+	tristate "emulex fibrechannel disk"
+	depends on PCI && SCSI && (PPC_PSERIES || PPC_ISERIES || IA64 || X86)
+
+config SCSI_LPFNDD
+	tristate "emulex fibrechannel net"
+	depends on PCI && SCSI && (PPC_PSERIES || PPC_ISERIES || IA64 || X86)
+
 config SCSI_IPR
 	tristate "IBM Power Linux RAID adapter support"
 	depends on PCI && SCSI
diff -p -purNX /suse/olh/kernel/kernel_exclude.txt linux-2.6.3.emulex/drivers/scsi/Makefile linux-2.6.3/drivers/scsi/Makefile
--- linux-2.6.3.emulex/drivers/scsi/Makefile	2004-03-01 13:16:05.000000000 +0100
+++ linux-2.6.3/drivers/scsi/Makefile	2004-03-01 14:59:45.000000000 +0100
@@ -116,6 +116,8 @@ obj-$(CONFIG_SCSI_SATA_PROMISE)	+= libat
 obj-$(CONFIG_SCSI_SATA_SIL)	+= libata.o sata_sil.o
 obj-$(CONFIG_SCSI_SATA_VIA)	+= libata.o sata_via.o
 obj-$(CONFIG_SCSI_IBMVSCSI)	+= ibmvscsi/
+obj-$(CONFIG_SCSI_LPFCDD)	+= lpfc/
+obj-$(CONFIG_SCSI_LPFNDD)	+= lpfc/
 
 obj-$(CONFIG_ARM)		+= arm/
 
