diff -u linux-2.6.4/arch/i386/kernel/cpu/cpufreq/Kconfig-o linux-2.6.4/arch/i386/kernel/cpu/cpufreq/Kconfig
--- linux-2.6.4/arch/i386/kernel/cpu/cpufreq/Kconfig-o	2004-03-11 03:55:23.000000000 +0100
+++ linux-2.6.4/arch/i386/kernel/cpu/cpufreq/Kconfig	2004-03-26 05:44:45.996115496 +0100
@@ -98,6 +98,17 @@
 
 	  If in doubt, say N.
 
+config X86_POWERNOW_K8_ACPI
+	bool "AMD Opteron/Athlon64 PowerNow! using ACPI"
+	depends on CPU_FREQ && EXPERIMENTAL && ACPI && X86_POWERNOW_K8
+	help
+	  This adds the CPUFreq driver for mobile AMD Opteron/Athlon64 processors.
+	  This version uses information from ACPI.
+
+	  For details, take a look at linux/Documentation/cpu-freq. 
+
+	  If in doubt, say N.
+
 config X86_GX_SUSPMOD
 	tristate "Cyrix MediaGX/NatSemi Geode Suspend Modulation"
 	depends on CPU_FREQ
diff -u linux-2.6.4/arch/x86_64/kernel/cpufreq/Kconfig-o linux-2.6.4/arch/x86_64/kernel/cpufreq/Kconfig
--- linux-2.6.4/arch/x86_64/kernel/cpufreq/Kconfig-o	2004-03-26 05:42:49.757978986 +0100
+++ linux-2.6.4/arch/x86_64/kernel/cpufreq/Kconfig	2004-03-26 05:44:41.101951819 +0100
@@ -32,13 +32,26 @@
        depends on CPU_FREQ
 
 config X86_POWERNOW_K8
-	tristate "AMD Opteron/Athlon64 PowerNow!"
+	tristate "AMD Opteron/Athlon64 PowerNow! using PST"
 	depends on CPU_FREQ_TABLE
 	help
 	  This adds the CPUFreq driver for mobile AMD Opteron/Athlon64 processors.
+	  This version uses the PST tables.
 
 	  For details, take a look at <file:Documentation/cpu-freq/>. 
 
 	  If in doubt, say N.
 
+config X86_POWERNOW_K8_ACPI
+	bool "AMD Opteron/Athlon64 PowerNow! using ACPI"
+	depends on CPU_FREQ && EXPERIMENTAL && ACPI && X86_POWERNOW_K8
+	help
+	  This adds the CPUFreq driver for mobile AMD Opteron/Athlon64 processors.
+	  This version uses information from ACPI.
+
+	  For details, take a look at linux/Documentation/cpu-freq. 
+
+	  If in doubt, say N.
+
+
 endmenu
