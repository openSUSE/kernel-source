From: Alexey Dobriyan <adobriyan@gmail.com>
Subject: ipw2200: fix ->eeprom[EEPROM_VERSION] check

priv->eeprom is a pointer.

Signed-off-by: Alexey Dobriyan <adobriyan@gmail.com>
Acked-by: okir@suse.de
 drivers/net/wireless/ipw2200.c |    2 +-
 1 files changed, 1 insertion(+), 1 deletion(-)

Index: build/drivers/net/wireless/ipw2200.c
===================================================================
--- build.orig/drivers/net/wireless/ipw2200.c
+++ build/drivers/net/wireless/ipw2200.c
@@ -2453,7 +2453,7 @@ static void ipw_eeprom_init_sram(struct 
 	   copy.  Otherwise let the firmware know to perform the operation
 	   on its own.
 	 */
-	if ((priv->eeprom + EEPROM_VERSION) != 0) {
+	if (priv->eeprom[EEPROM_VERSION] != 0) {
 		IPW_DEBUG_INFO("Writing EEPROM data into SRAM\n");
 
 		/* write the eeprom data to sram */
