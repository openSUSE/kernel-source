Evil hack to get parallel build (make -j) to work.

Index: linux-2.6.3/drivers/infiniband/mad/Makefile
===================================================================
--- linux-2.6.3.orig/drivers/infiniband/mad/Makefile	2004-02-28 10:14:44.868006161 +0100
+++ linux-2.6.3/drivers/infiniband/mad/Makefile	2004-02-28 10:15:21.731017420 +0100
@@ -1,3 +1,11 @@
+ifneq ($(KBUILD_MODULES),)
+$(shell lib=$(obj)/../core/ib_packet_lib.o \; \
+  while ! [ -e $$lib ] \; do \
+    echo "Waiting for $$lib" >&2 \; \
+    sleep 1 \; \
+  done)
+endif
+
 EXTRA_CFLAGS += \
     -Idrivers/infiniband/include \
     -Idrivers/infiniband/hw/mellanox-hca/include \
