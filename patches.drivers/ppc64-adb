From: Olaf Hering <olh@suse.de>
Subject: enable mouse button emulation also for G5
Patch-mainline: never

fix compile errors

 drivers/macintosh/Kconfig  |    2 +-
 drivers/macintosh/adb.c    |    4 ++++
 drivers/macintosh/adbhid.c |    6 +++++-
 3 files changed, 10 insertions(+), 2 deletions(-)

--- a/drivers/macintosh/Kconfig	2007-10-31 17:43:57.000000000 -0400
+++ b/drivers/macintosh/Kconfig	2007-10-31 17:43:58.000000000 -0400
@@ -13,7 +13,7 @@ if MACINTOSH_DRIVERS
 
 config ADB
 	bool "Apple Desktop Bus (ADB) support"
-	depends on MAC || (PPC_PMAC && PPC32)
+	depends on MAC || PPC_PMAC
 	help
 	  Apple Desktop Bus (ADB) support is for support of devices which
 	  are connected to an ADB port.  ADB devices tend to have 4 pins.
--- a/drivers/macintosh/adb.c	2007-10-31 17:43:31.000000000 -0400
+++ b/drivers/macintosh/adb.c	2007-10-31 17:43:58.000000000 -0400
@@ -290,6 +290,10 @@ int __init adb_init(void)
 	if (!machine_is(chrp) && !machine_is(powermac))
 		return 0;
 #endif
+#ifdef CONFIG_PPC64
+	if (!machine_is(powermac))
+		return 0;
+#endif
 #ifdef CONFIG_MAC
 	if (!MACH_IS_MAC)
 		return 0;
--- a/drivers/macintosh/adbhid.c	2007-10-31 17:43:31.000000000 -0400
+++ b/drivers/macintosh/adbhid.c	2007-10-31 17:43:58.000000000 -0400
@@ -1180,10 +1180,14 @@ init_ms_a3(int id)
 
 static int __init adbhid_init(void)
 {
-#ifndef CONFIG_MAC
+#ifdef CONFIG_PPC32
 	if (!machine_is(chrp) && !machine_is(powermac))
 		return 0;
 #endif
+#ifdef CONFIG_PPC64
+	if (!machine_is(powermac))
+		return 0;
+#endif
 
 	led_request.complete = 1;
 
