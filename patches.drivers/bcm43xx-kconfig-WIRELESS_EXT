From: olh@suse.de
Subject: undefined symbol
Patch-mainline: yes

wireless_send_event() exists only if CONFIG_NET_RADIO is enabled.
Hide softmac in this case, similar to all other users of NET_RADIO.

Signed-off-by: Olaf Hering <olh@suse.de>

---
 net/ieee80211/softmac/Kconfig |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: linux-2.6.16/net/ieee80211/softmac/Kconfig
===================================================================
--- linux-2.6.16.orig/net/ieee80211/softmac/Kconfig
+++ linux-2.6.16/net/ieee80211/softmac/Kconfig
@@ -1,7 +1,7 @@
 config IEEE80211_SOFTMAC
 	tristate "Software MAC add-on to the IEEE 802.11 networking stack"
 	depends on IEEE80211 && EXPERIMENTAL
-	select WIRELESS_EXT
+	depends on NET_RADIO
 	---help---
 	This option enables the hardware independent software MAC addon
 	for the IEEE 802.11 networking stack.
