Subject: Fix endian issue with keyspan_remote driver
From: Greg Kroah-Hartman <gregkh@suse.de>
Status: upstream

Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>

---
 drivers/usb/input/keyspan_remote.c |    5 -----
 1 files changed, 5 deletions(-)

--- gregkh-2.6.orig/drivers/usb/input/keyspan_remote.c	2005-08-08 15:17:03.000000000 -0700
+++ gregkh-2.6/drivers/usb/input/keyspan_remote.c	2005-08-08 15:17:09.000000000 -0700
@@ -431,11 +431,6 @@
 	struct usb_endpoint_descriptor *endpoint;
 	struct usb_device *udev = usb_get_dev(interface_to_usbdev(interface));
 
-	/* See if the offered device matches what we can accept */
-	if ((udev->descriptor.idVendor != USB_KEYSPAN_VENDOR_ID) ||
-	    (udev->descriptor.idProduct != USB_KEYSPAN_PRODUCT_UIA11) )
-		return -ENODEV;
-
 	/* allocate memory for our device state and initialize it */
 	remote = kmalloc(sizeof(*remote), GFP_KERNEL);
 	if (remote == NULL) {
