From: Takashi Iwai <tiwai@suse.de>
Subject: Use full-digital model as default for CMI9880
Patch-mainline: not yet
References: 

Use full-digital model as default for CMI9880 codecs.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

--- linux/sound/pci/hda/patch_cmedia.c	22 Feb 2005 14:00:35 -0000	1.3
+++ linux/sound/pci/hda/patch_cmedia.c	15 Mar 2005 15:40:40 -0000
@@ -565,7 +565,7 @@
 	spec->board_config = snd_hda_check_board_config(codec, cmi9880_cfg_tbl);
 	if (spec->board_config < 0) {
 		snd_printd(KERN_INFO "hda_codec: Unknown model for CMI9880\n");
-		spec->board_config = CMI_MINIMAL;
+		spec->board_config = CMI_FULL_DIG; /* try everything */
 	}
 
 	switch (spec->board_config) {
