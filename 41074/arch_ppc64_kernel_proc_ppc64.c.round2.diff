--- linux-2.6.7.proc.orig/arch/ppc64/kernel/proc_ppc64.c
+++ linux-2.6.7.proc/arch/ppc64/kernel/proc_ppc64.c
@@ -167,7 +167,7 @@ static ssize_t page_map_read( struct fil
 	loff_t pos = *ppos;
 	struct proc_dir_entry *dp = PDE(file->f_dentry->d_inode);
 
-	if ( pos >= dp->size )
+	if ( pos < 0 || pos >= dp->size )
 		return 0;
 	if ( nbytes >= dp->size )
 		nbytes = dp->size;
