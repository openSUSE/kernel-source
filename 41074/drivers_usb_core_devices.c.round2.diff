--- linux-2.6.7.proc.orig/drivers/usb/core/devices.c
+++ linux-2.6.7.proc/drivers/usb/core/devices.c
@@ -599,7 +599,6 @@ static ssize_t usb_device_read(struct fi
 		ret = usb_device_dump(&buf, &nbytes, &skip_bytes, ppos, bus->root_hub, bus, 0, 0, 0);
 		up(&bus->root_hub->serialize);
 		if (ret < 0) {
-			up(&usb_bus_list_lock);
 			goto error;
 		}
 		total_written += ret;
