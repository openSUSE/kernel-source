--- linux-2.6.7.proc.orig/drivers/scsi/osst.c
+++ linux-2.6.7.proc/drivers/scsi/osst.c
@@ -3508,13 +3508,13 @@ static ssize_t osst_read(struct file * f
 	 * access to the device is prohibited.
 	 */
 	if( !scsi_block_when_processing_errors(STp->device) ) {
-		retval = (-ESPIPE);
+		retval = (-ENXIO);
 		goto out;
 	}
 	
 	if (ppos != &filp->f_pos) {
 		/* "A request was outside the capabilities of the device." */
-		retval = (-ENXIO);
+		retval = (-ESPIPE);
 		goto out;
 	}
 
