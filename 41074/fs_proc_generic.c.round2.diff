--- linux-2.6.7.proc.orig/fs/proc/generic.c
+++ linux-2.6.7.proc/fs/proc/generic.c
@@ -61,7 +61,7 @@ proc_file_read(struct file *file, char _
 
 	while ((nbytes > 0) && !eof) {
 		count = min_t(ssize_t, PROC_BLOCK_SIZE, nbytes);
-		if ((unsigned)pos > INT_MAX)
+		if (pos != (unsigned) pos || pos > INT_MAX)
 			break;
  
 		start = NULL;
