--- linux-2.6.7.proc.orig/net/sunrpc/cache.c
+++ linux-2.6.7.proc/net/sunrpc/cache.c
@@ -1183,7 +1183,7 @@ static ssize_t read_flush(struct file *f
 	if (copy_to_user(buf, (void*)(tbuf+p), len))
 		len = -EFAULT;
 	else
-		*ppos += len;
+		*ppos = p + len;
 	return len;
 }
 
