Index: linux-2.6.5/scripts/Makefile.modinst
===================================================================
--- linux-2.6.5.orig/scripts/Makefile.modinst
+++ linux-2.6.5/scripts/Makefile.modinst
@@ -29,11 +29,12 @@ quiet_cmd_modules_install = INSTALL $@
 $(filter-out ../% /%,$(modules)):
 	$(call cmd,modules_install)
 
-# Modules built outside just go into extra
+# Modules built outside just go into extra by default
+MOD_DIR ?= extra
 
 quiet_cmd_modules_install_extra = INSTALL $@
-      cmd_modules_install_extra = mkdir -p $(MODLIB)/extra; \
-			    	  cp $@ $(MODLIB)/extra
+      cmd_modules_install_extra = mkdir -p $(MODLIB)/$(MOD_DIR); \
+			    	  cp $@ $(MODLIB)/$(MOD_DIR)
 
 $(filter     ../% /%,$(modules)):
 	$(call cmd,modules_install_extra)
