Index: linux-2.6.5/scripts/mkcompile_h
===================================================================
--- linux-2.6.5.orig/scripts/mkcompile_h
+++ linux-2.6.5/scripts/mkcompile_h
@@ -27,7 +27,12 @@ fi
 
 UTS_VERSION="#$VERSION"
 if [ -n "$SMP" ] ; then UTS_VERSION="$UTS_VERSION SMP"; fi
-UTS_VERSION="$UTS_VERSION `LC_ALL=C LANG=C date`"
+if [ -n "$BUILD_TIMESTAMP" ]; then
+  TIMESTAMP="`LC_ALL=C LANG=C date -d "$BUILD_TIMESTAMP"`"
+else
+   TIMESTAMP="`LC_ALL=C LANG=C date`"
+fi
+UTS_VERSION="$UTS_VERSION $TIMESTAMP"
 
 # Truncate to maximum length
 
