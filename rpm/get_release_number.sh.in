#! /bin/sh

if [ "$3" = kernel-dummy ]; then
    prefix=@RELEASE_PREFIX@
    suffix=$2
    while [ "$suffix" != "${suffix#[^0-9]*.}" ]; do
	suffix=${suffix#[^0-9]*.}
    done
    echo $prefix$suffix
else
    echo "pkg:kernel-dummy"
fi
