#!/bin/sh

set -e

# Check the patches
scripts/guards --path "$(echo patches.* | sed -e 's/ /:/g')" \
	       --check \
	       < series.conf

# Check the configuration files
scripts/guards --prefix config --check < config.conf
