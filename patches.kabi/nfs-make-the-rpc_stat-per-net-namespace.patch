From: NeilBrown <neilb@suse.de>
Subject: kabi fix for nfs: make the rpc_stat per net namespace
Patch-mainline: Never, kabi
References: git-fixes

Set the flag so that the new stats field is recongnised.

---
 fs/nfs/client.c |    1 +
 1 file changed, 1 insertion(+)

--- a/fs/nfs/client.c
+++ b/fs/nfs/client.c
@@ -514,6 +514,7 @@ int nfs_create_rpc_client(struct nfs_cli
 		.nodename	= cl_init->nodename,
 		.program	= &nfs_program,
 		.stats		= &nn->rpcstats,
+		.flags		= RPC_CLNT_CREATE_STATS,
 		.version	= clp->rpc_ops->version,
 		.authflavor	= flavor,
 		.cred		= cl_init->cred,
