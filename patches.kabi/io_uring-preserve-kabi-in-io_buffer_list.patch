From: Gabriel Krisman Bertazi <krisman@suse.de>
Subject: io_uring: Preserve kABI in io_buffer_list
Patch-mainline: Never, kabi
References: git-fixes

Field ->is_buf_ring was removed and it was followed by a 3 byte hole.
Anotate and preserve that gap for future use.

---
 io_uring/kbuf.h |    7 -------
 1 file changed, 7 deletions(-)

--- a/io_uring/kbuf.h
+++ b/io_uring/kbuf.h
@@ -33,6 +33,13 @@ struct io_buffer_list {
 #endif
 	/* ring mapped provided buffers, but mmap'ed by application */
 	__u8 is_mmap;
+
+	/* is_buf_ring is unsused.  preserved only for kABI purposes */
+#ifdef __GENKSYMS__
+	__u8 is_buf_ring;
+#else
+	__u8 padding[4];
+#endif
 };
 
 struct io_buffer {
