From: Michal Kubecek <mkubecek@suse.cz>
Date: Wed, 2 Sep 2026 13:42:13 +0200
Subject: kabi: move skb_dev out of struct nf_queue_entry
Patch-mainline: Never, kabi workaround
References: CVE-2026-52912 bsc#1269000

Backport of mainline commit e196115ec330 ("netfilter: nf_queue: hold bridge
skb->dev while queued") inserts a pointer skb_dev into kabi protected
struct nf_queue_entry. As additional data is expected to be placed right
after the structure, moving the pointer to the end of the structure would
be too complicated.

Instances of struct nf_queue_entry are only allocated in __nf_queue() and
freed either in error paths of the same function or nf_queue_entry_free()
helper. Therefore we can put the structure into a wrapper that would be
allocated and freed instead.

SLE12-SP5: the 4.12 code does not have the nf_queue_entry_free() code yet,
introduce a helper and use it instead of kfree(entry) in all reinject
paths.

Signed-off-by: Michal Kubecek <mkubecek@suse.cz>
Reviewed-by: Jan Kara <jack@suse.cz>
---
 include/net/netfilter/nf_queue.h | 26 +++++++++++++++++++++++++-
 net/netfilter/nf_queue.c         | 20 +++++++++++---------
 net/netfilter/nfnetlink_queue.c  |  7 ++++---
 3 files changed, 40 insertions(+), 13 deletions(-)

--- a/include/net/netfilter/nf_queue.h
+++ b/include/net/netfilter/nf_queue.h
@@ -9,7 +9,6 @@
 struct nf_queue_entry {
 	struct list_head	list;
 	struct sk_buff		*skb;
-	struct net_device	*skb_dev;
 	unsigned int		id;
 
 	struct nf_hook_state	state;
@@ -21,6 +20,31 @@ struct nf_queue_entry {
 
 #define nf_queue_entry_reroute(x) ((void *)x + sizeof(struct nf_queue_entry))
 
+struct nf_queue_entry_kabi_wrapper {
+	struct net_device	*skb_dev;
+	struct nf_queue_entry	entry;
+};
+
+/* make sure allocated size matches what was expected without the wrapper */
+static_assert(sizeof(struct nf_queue_entry_kabi_wrapper) ==
+	      sizeof(struct net_device *) + sizeof(struct nf_queue_entry));
+
+static inline struct net_device *nf_queue_entry_skbdev(const struct nf_queue_entry *e)
+{
+	const struct nf_queue_entry_kabi_wrapper *wrapper =
+		container_of(e, const struct nf_queue_entry_kabi_wrapper, entry);
+
+	return wrapper->skb_dev;
+}
+
+static inline void nf_queue_entry_free(struct nf_queue_entry *entry)
+{
+	struct nf_queue_entry_kabi_wrapper *wrapper =
+		container_of(entry, struct nf_queue_entry_kabi_wrapper, entry);
+
+	kfree(wrapper);
+}
+
 /* Packet queuing */
 struct nf_queue_handler {
 	int		(*outfn)(struct nf_queue_entry *entry,
--- a/net/netfilter/nf_queue.c
+++ b/net/netfilter/nf_queue.c
@@ -49,8 +49,8 @@ void nf_queue_entry_release_refs(struct nf_queue_entry *entry)
 	struct nf_hook_state *state = &entry->state;
 
 	/* Release those devices we held, or Alexey will kill me. */
-	if (entry->skb_dev)
-		dev_put(entry->skb_dev);
+	if (nf_queue_entry_skbdev(entry))
+		dev_put(nf_queue_entry_skbdev(entry));
 	if (state->in)
 		dev_put(state->in);
 	if (state->out)
@@ -80,8 +80,8 @@ bool __nf_queue_entry_get_refs(struct nf_queue_entry *entry)
 	if (state->sk && !atomic_inc_not_zero(&state->sk->sk_refcnt))
 		return false;
 
-	if (entry->skb_dev)
-		dev_hold(entry->skb_dev);
+	if (nf_queue_entry_skbdev(entry))
+		dev_hold(nf_queue_entry_skbdev(entry));
 	if (state->in)
 		dev_hold(state->in);
 	if (state->out)
@@ -146,6 +146,7 @@ static int __nf_queue(struct sk_buff *skb, const struct nf_hook_state *state,
 		      struct nf_hook_entry *hook_entry, unsigned int queuenum)
 {
 	int status = -ENOENT;
+	struct nf_queue_entry_kabi_wrapper *wrapper = NULL;
 	struct nf_queue_entry *entry = NULL;
 	const struct nf_afinfo *afinfo;
 	const struct nf_queue_handler *qh;
@@ -162,11 +163,12 @@ static int __nf_queue(struct sk_buff *skb, const struct nf_hook_state *state,
 	if (!afinfo)
 		goto err;
 
-	entry = kmalloc(sizeof(*entry) + afinfo->route_key_size, GFP_ATOMIC);
-	if (!entry) {
+	wrapper = kmalloc(sizeof(*wrapper) + afinfo->route_key_size, GFP_ATOMIC);
+	if (!wrapper) {
 		status = -ENOMEM;
 		goto err;
 	}
+	entry = &wrapper->entry;
 
 	if (skb_dst(skb) && !skb_dst_force(skb)) {
 		status = -ENETDOWN;
@@ -175,11 +177,11 @@ static int __nf_queue(struct sk_buff *skb, const struct nf_hook_state *state,
 
 	*entry = (struct nf_queue_entry) {
 		.skb	= skb,
-		.skb_dev = skb->dev,
 		.state	= *state,
 		.hook	= hook_entry,
 		.size	= sizeof(*entry) + afinfo->route_key_size,
 	};
+	wrapper->skb_dev = skb->dev;
 
 	if (!__nf_queue_entry_get_refs(entry)) {
 		status = -ENOTCONN;
@@ -196,7 +198,7 @@ static int __nf_queue(struct sk_buff *skb, const struct nf_hook_state *state,
 	return 0;
 
 err:
-	kfree(entry);
+	kfree(wrapper);
 	return status;
 }
 
@@ -288,6 +290,6 @@ void nf_reinject(struct nf_queue_entry *entry, unsigned int verdict)
 		kfree_skb(skb);
 	}
 
-	kfree(entry);
+	nf_queue_entry_free(entry);
 }
 EXPORT_SYMBOL(nf_reinject);
--- a/net/netfilter/nfnetlink_queue.c
+++ b/net/netfilter/nfnetlink_queue.c
@@ -729,7 +729,7 @@ nf_queue_entry_dup(struct nf_queue_entry *e)
 	if (__nf_queue_entry_get_refs(entry))
 		return entry;
 
-	kfree(entry);
+	nf_queue_entry_free(entry);
 	return NULL;
 }
 
@@ -757,7 +757,7 @@ static void nf_bridge_adjust_segmented_data(struct sk_buff *skb)
 static void free_entry(struct nf_queue_entry *entry)
 {
 	nf_queue_entry_release_refs(entry);
-	kfree(entry);
+	nf_queue_entry_free(entry);
 }
 
 static int
@@ -925,7 +925,8 @@ nfqnl_set_mode(struct nfqnl_instance *queue,
 static int
 dev_cmp(struct nf_queue_entry *entry, unsigned long ifindex)
 {
-	if (entry->skb_dev && entry->skb_dev->ifindex == ifindex)
+	if (nf_queue_entry_skbdev(entry) &&
+	    nf_queue_entry_skbdev(entry)->ifindex == ifindex)
 		return 1;
 	if (entry->state.in)
 		if (entry->state.in->ifindex == ifindex)
