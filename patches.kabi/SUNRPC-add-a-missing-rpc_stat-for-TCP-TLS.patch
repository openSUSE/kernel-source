From: NeilBrown <neilb@suse.de>
Subject: kabi fix for SUNRPC: add a missing rpc_stat for TCP TLS
Patch-mainline: Never, kabi
References: git-fixes

Set the flag so that the new stats field is recongnised.
---
 net/sunrpc/xprtsock.c |    1 +
 1 file changed, 1 insertion(+)

--- a/net/sunrpc/xprtsock.c
+++ b/net/sunrpc/xprtsock.c
@@ -2627,6 +2627,7 @@ static void xs_tcp_tls_setup_socket(stru
 			.policy		= RPC_XPRTSEC_NONE,
 		},
 		.stats		= upper_clnt->cl_stats,
+		.flags		= RPC_CLNT_CREATE_STATS,
 	};
 	unsigned int pflags = current->flags;
 	struct rpc_clnt *lower_clnt;
