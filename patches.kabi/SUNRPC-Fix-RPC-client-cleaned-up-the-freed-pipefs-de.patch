From: NeilBrown <neilb@suse.de>
Subject: kabi fix for  SUNRPC: Fix RPC client cleaned up the freed pipefs dentries
Patch-mainline: never, kabi
References: git-fixes

As struct rpc_clnt is only allocated in sunrpc code, it is safe to make the structure
a little larger - no other modules can notice.

Signed-off-by: NeilBrown <neilb@suse.de>
---
 include/linux/sunrpc/clnt.h |    2 ++
 1 file changed, 2 insertions(+)

--- a/include/linux/sunrpc/clnt.h
+++ b/include/linux/sunrpc/clnt.h
@@ -81,7 +81,9 @@ struct rpc_clnt {
 		struct work_struct	cl_work;
 	};
 	const struct cred	*cl_cred;
+#ifndef __GENKSYMS__
 	struct super_block *pipefs_sb;
+#endif
 };
 
 /*
