From: Steffen Jaeckel <sjaeckel@suse.de>
Subject: Fix race for duplicate reqsk on identical SYN
Patch-mainline: Never, kABI workaround
References: CVE-2026-53260 bsc#1269731

This is a kABI fix for upstream commit ff46e3b44219 ("Fix race
for duplicate reqsk on identical SYN") which changed the API signature
of inet_csk_reqsk_queue_hash_add() to return a bool.

1. Rename the new version and use that.
2. Re-introduce the original API which simply calls the renamed new one and
   discards the return value.

Signed-off-by: Steffen Jaeckel <sjaeckel@suse.de>
Reviewed-by: Jan Kara <jack@suse.cz>
---
 include/net/inet_connection_sock.h |  6 ++++--
 net/dccp/ipv4.c                    |  2 +-
 net/dccp/ipv6.c                    |  2 +-
 net/ipv4/inet_connection_sock.c    | 10 +++++++++-
 net/ipv4/tcp_input.c               |  4 ++--
 5 files changed, 17 insertions(+), 7 deletions(-)

diff --git a/include/net/inet_connection_sock.h b/include/net/inet_connection_sock.h
index 437112f922aa..7d416bba4c56 100644
--- a/include/net/inet_connection_sock.h
+++ b/include/net/inet_connection_sock.h
@@ -264,8 +264,10 @@ struct dst_entry *inet_csk_route_child_sock(const struct sock *sk,
 struct sock *inet_csk_reqsk_queue_add(struct sock *sk,
 				      struct request_sock *req,
 				      struct sock *child);
-bool inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
-				   unsigned long timeout);
+void inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
+                                   unsigned long timeout);
+bool __inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
+                                     unsigned long timeout);
 struct sock *inet_csk_complete_hashdance(struct sock *sk, struct sock *child,
 					 struct request_sock *req,
 					 bool own_req);
diff --git a/net/dccp/ipv4.c b/net/dccp/ipv4.c
index aacd15cf6679..89e2805d6f46 100644
--- a/net/dccp/ipv4.c
+++ b/net/dccp/ipv4.c
@@ -656,7 +656,7 @@ int dccp_v4_conn_request(struct sock *sk, struct sk_buff *skb)
 	if (dccp_v4_send_response(sk, req))
 		goto drop_and_free;
 
-	if (unlikely(!inet_csk_reqsk_queue_hash_add(sk, req, DCCP_TIMEOUT_INIT)))
+	if (unlikely(!__inet_csk_reqsk_queue_hash_add(sk, req, DCCP_TIMEOUT_INIT)))
 		reqsk_free(req);
 	else
 		reqsk_put(req);
diff --git a/net/dccp/ipv6.c b/net/dccp/ipv6.c
index 34aa2c02ad88..ae8d7c9d3dc3 100644
--- a/net/dccp/ipv6.c
+++ b/net/dccp/ipv6.c
@@ -398,7 +398,7 @@ static int dccp_v6_conn_request(struct sock *sk, struct sk_buff *skb)
 	if (dccp_v6_send_response(sk, req))
 		goto drop_and_free;
 
-	if (unlikely(!inet_csk_reqsk_queue_hash_add(sk, req, DCCP_TIMEOUT_INIT)))
+	if (unlikely(!__inet_csk_reqsk_queue_hash_add(sk, req, DCCP_TIMEOUT_INIT)))
 		reqsk_free(req);
 	else
 		reqsk_put(req);
diff --git a/net/ipv4/inet_connection_sock.c b/net/ipv4/inet_connection_sock.c
index 33c20fe2e077..6a7d97a90930 100644
--- a/net/ipv4/inet_connection_sock.c
+++ b/net/ipv4/inet_connection_sock.c
@@ -1150,7 +1150,7 @@ static bool reqsk_queue_hash_req(struct request_sock *req,
 	return true;
 }
 
-bool inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
+bool __inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
 				   unsigned long timeout)
 {
 	if (!reqsk_queue_hash_req(req, timeout))
@@ -1159,6 +1159,14 @@ bool inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
 	inet_csk_reqsk_queue_added(sk);
 	return true;
 }
+EXPORT_SYMBOL_GPL(__inet_csk_reqsk_queue_hash_add);
+
+void inet_csk_reqsk_queue_hash_add(struct sock *sk, struct request_sock *req,
+                                   unsigned long timeout)
+{
+	if (!__inet_csk_reqsk_queue_hash_add(sk, req, timeout))
+		refcount_set(&req->rsk_refcnt, 1);
+}
 EXPORT_SYMBOL_GPL(inet_csk_reqsk_queue_hash_add);
 
 static void inet_clone_ulp(const struct request_sock *req, struct sock *newsk,
diff --git a/net/ipv4/tcp_input.c b/net/ipv4/tcp_input.c
index 416823148430..1f89b6d1ead7 100644
--- a/net/ipv4/tcp_input.c
+++ b/net/ipv4/tcp_input.c
@@ -7205,8 +7205,8 @@ int tcp_conn_request(struct request_sock_ops *rsk_ops,
 		tcp_rsk(req)->tfo_listener = false;
 		if (!want_cookie) {
 			req->timeout = tcp_timeout_init((struct sock *)req);
-			if (unlikely(!inet_csk_reqsk_queue_hash_add(sk, req,
-								    req->timeout))) {
+			if (unlikely(!__inet_csk_reqsk_queue_hash_add(sk, req,
+			                                              req->timeout))) {
 				reqsk_free(req);
 				dst_release(dst);
 				return 0;
-- 
2.54.0

