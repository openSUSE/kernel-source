From: Michal Kubecek <mkubecek@suse.cz>
Date: Thu, 11 Jun 2026 00:53:38 +0200
Subject: kabi: revert kabi breaking changes from bsc#1264610 fix
Patch-mainline: Never, kabi workaround
References: CVE-2026-43198 bsc#1264610

Backport of mainline commit 858d2a4f67ff ("tcp: fix potential race in
tcp_v6_syn_recv_sock()") adds new parameter to ->syn_recv_sock() callback
in struct inet_connection_sock_af_ops.

This parameter itself is a function which serves the only purpose: to be
called at the right place in tcp_v4_syn_recv_sock() if not null. However,

  - it is only used to be called if not null
  - it is not used in any other ->syn_recv_sock() callback
  - it is only not null when called from tcp_v6_syn_recv_sock()
  - tcp_v6_syn_recv_sock() calls tcp_v4_syn_recv_sock() directly
    (not via ops structure pointer)

Therefore we can restore kABI by

  - reverting changes to ->syn_recv_sock() parameter list
  - renaming patched tcp_v4_syn_recv_sock() to a different name
  - making tcp_v4_syn_recv_sock() a wrapper which calls patched with null
  - call the patched function from tcp_v6_syn_recv_sock() instead

This preserves kABI and behaviour of all ->syn_recv_sock() callbacks while
allowing tcp_v6_syn_recv_sock() call the patched function which calls
tcp_v6_mapped_child_init() if needed (when TCPv4 connection is received on
an IPv6 listening socket).

Signed-off-by: Michal Kubecek <mkubecek@suse.cz>
Reviewed-by: Petr Pavlu <petr.pavlu@suse.com>
---
 include/net/inet_connection_sock.h |  4 +---
 include/net/tcp.h                  | 11 ++++++++---
 net/ipv4/syncookies.c              |  2 +-
 net/ipv4/tcp_fastopen.c            |  2 +-
 net/ipv4/tcp_ipv4.c                | 30 +++++++++++++++++++++++-------
 net/ipv4/tcp_minisocks.c           |  2 +-
 net/ipv6/tcp_ipv6.c                | 14 ++++++--------
 net/mptcp/subflow.c                |  6 ++----
 net/smc/af_smc.c                   |  6 ++----
 9 files changed, 45 insertions(+), 32 deletions(-)

--- a/include/net/inet_connection_sock.h
+++ b/include/net/inet_connection_sock.h
@@ -42,9 +42,7 @@ struct inet_connection_sock_af_ops {
 				      struct request_sock *req,
 				      struct dst_entry *dst,
 				      struct request_sock *req_unhash,
-				      bool *own_req,
-				      void (*opt_child_init)(struct sock *newsk,
-							     const struct sock *sk));
+				      bool *own_req);
 	u16	    net_header_len;
 	u16	    net_frag_header_len;
 	u16	    sockaddr_len;
--- a/include/net/tcp.h
+++ b/include/net/tcp.h
@@ -461,13 +461,18 @@ struct sock *tcp_create_openreq_child(const struct sock *sk,
 				      struct request_sock *req,
 				      struct sk_buff *skb);
 void tcp_ca_openreq_child(struct sock *sk, const struct dst_entry *dst);
+struct sock *tcp_v4_syn_recv_sock_bsc1264610(const struct sock *sk, struct sk_buff *skb,
+					     struct request_sock *req,
+					     struct dst_entry *dst,
+					     struct request_sock *req_unhash,
+					     bool *own_req,
+					     void (*opt_child_init)(struct sock *newsk,
+								    const struct sock *sk));
 struct sock *tcp_v4_syn_recv_sock(const struct sock *sk, struct sk_buff *skb,
 				  struct request_sock *req,
 				  struct dst_entry *dst,
 				  struct request_sock *req_unhash,
-				  bool *own_req,
-				  void (*opt_child_init)(struct sock *newsk,
-							 const struct sock *sk));
+				  bool *own_req);
 int tcp_v4_do_rcv(struct sock *sk, struct sk_buff *skb);
 int tcp_v4_connect(struct sock *sk, struct sockaddr *uaddr, int addr_len);
 int tcp_connect(struct sock *sk);
--- a/net/ipv4/syncookies.c
+++ b/net/ipv4/syncookies.c
@@ -199,7 +199,7 @@ struct sock *tcp_get_cookie_sock(struct sock *sk, struct sk_buff *skb,
 	bool own_req;
 
 	child = icsk->icsk_af_ops->syn_recv_sock(sk, skb, req, dst,
-						 NULL, &own_req, NULL);
+						 NULL, &own_req);
 	if (child) {
 		refcount_set(&req->rsk_refcnt, 1);
 		tcp_sk(child)->tsoffset = tsoff;
--- a/net/ipv4/tcp_fastopen.c
+++ b/net/ipv4/tcp_fastopen.c
@@ -246,7 +246,7 @@ static struct sock *tcp_fastopen_create_child(struct sock *sk,
 	bool own_req;
 
 	child = inet_csk(sk)->icsk_af_ops->syn_recv_sock(sk, skb, req, NULL,
-							 NULL, &own_req, NULL);
+							 NULL, &own_req);
 	if (!child)
 		return NULL;
 
--- a/net/ipv4/tcp_ipv4.c
+++ b/net/ipv4/tcp_ipv4.c
@@ -1554,14 +1554,19 @@ EXPORT_SYMBOL(tcp_v4_conn_request);
 /*
  * The three way handshake has completed - we got a valid synack -
  * now create the new socket.
+ *
+ * Function patched for bsc#1264610 provided under a different name, the
+ * unpatched one is just a wrapper calling it with null opt_child_init
+ * (i.e. preserving the original behaviour). This version is only called
+ * directly from tcp_v6_syn_recv_sock().
  */
-struct sock *tcp_v4_syn_recv_sock(const struct sock *sk, struct sk_buff *skb,
-				  struct request_sock *req,
-				  struct dst_entry *dst,
-				  struct request_sock *req_unhash,
-				  bool *own_req,
-				  void (*opt_child_init)(struct sock *newsk,
-							 const struct sock *sk))
+struct sock *tcp_v4_syn_recv_sock_bsc1264610(const struct sock *sk, struct sk_buff *skb,
+					     struct request_sock *req,
+					     struct dst_entry *dst,
+					     struct request_sock *req_unhash,
+					     bool *own_req,
+					     void (*opt_child_init)(struct sock *newsk,
+								    const struct sock *sk))
 {
 	struct inet_request_sock *ireq;
 	bool found_dup_sk = false;
@@ -1674,6 +1679,17 @@ struct sock *tcp_v4_syn_recv_sock(const struct sock *sk, struct sk_buff *skb,
 	tcp_done(newsk);
 	goto exit;
 }
+EXPORT_SYMBOL(tcp_v4_syn_recv_sock_bsc1264610);
+
+struct sock *tcp_v4_syn_recv_sock(const struct sock *sk, struct sk_buff *skb,
+				  struct request_sock *req,
+				  struct dst_entry *dst,
+				  struct request_sock *req_unhash,
+				  bool *own_req)
+{
+	return tcp_v4_syn_recv_sock_bsc1264610(sk, skb, req, dst, req_unhash,
+					       own_req, NULL);
+}
 EXPORT_SYMBOL(tcp_v4_syn_recv_sock);
 
 static struct sock *tcp_v4_cookie_check(struct sock *sk, struct sk_buff *skb)
--- a/net/ipv4/tcp_minisocks.c
+++ b/net/ipv4/tcp_minisocks.c
@@ -805,7 +805,7 @@ struct sock *tcp_check_req(struct sock *sk, struct sk_buff *skb,
 	 * socket is created, wait for troubles.
 	 */
 	child = inet_csk(sk)->icsk_af_ops->syn_recv_sock(sk, skb, req, NULL,
-							 req, &own_req, NULL);
+							 req, &own_req);
 	if (!child)
 		goto listen_overflow;
 
--- a/net/ipv6/tcp_ipv6.c
+++ b/net/ipv6/tcp_ipv6.c
@@ -1180,7 +1180,7 @@ static void tcp_v6_restore_cb(struct sk_buff *skb)
 		sizeof(struct inet6_skb_parm));
 }
 
-/* Called from tcp_v4_syn_recv_sock() for v6_mapped children. */
+/* Called from tcp_v4_syn_recv_sock_bsc1264610() for v6_mapped children. */
 static void tcp_v6_mapped_child_init(struct sock *newsk, const struct sock *sk)
 {
 	struct inet_sock *newinet = inet_sk(newsk);
@@ -1206,7 +1206,7 @@ static void tcp_v6_mapped_child_init(struct sock *newsk, const struct sock *sk)
 	newnp->pktoptions  = NULL;
 	newnp->opt	   = NULL;
 
-	/* tcp_v4_syn_recv_sock() has initialized newinet->mc_{index,ttl} */
+	/* tcp_v4_syn_recv_sock_bsc1264610() has initialized newinet->mc_{index,ttl} */
 	newnp->mcast_oif   = newinet->mc_index;
 	newnp->mcast_hops  = newinet->mc_ttl;
 
@@ -1219,9 +1219,7 @@ static struct sock *tcp_v6_syn_recv_sock(const struct sock *sk, struct sk_buff *
 					 struct request_sock *req,
 					 struct dst_entry *dst,
 					 struct request_sock *req_unhash,
-					 bool *own_req,
-					 void (*opt_child_init)(struct sock *newsk,
-								const struct sock *sk))
+					 bool *own_req)
 {
 	struct inet_request_sock *ireq;
 	struct ipv6_pinfo *newnp;
@@ -1238,9 +1236,9 @@ static struct sock *tcp_v6_syn_recv_sock(const struct sock *sk, struct sk_buff *
 	struct flowi6 fl6;
 
 	if (skb->protocol == htons(ETH_P_IP))
-		return tcp_v4_syn_recv_sock(sk, skb, req, dst,
-					    req_unhash, own_req,
-					    tcp_v6_mapped_child_init);
+		return tcp_v4_syn_recv_sock_bsc1264610(sk, skb, req, dst,
+						       req_unhash, own_req,
+						       tcp_v6_mapped_child_init);
 	ireq = inet_rsk(req);
 
 	if (sk_acceptq_is_full(sk))
--- a/net/mptcp/subflow.c
+++ b/net/mptcp/subflow.c
@@ -752,9 +752,7 @@ static struct sock *subflow_syn_recv_sock(const struct sock *sk,
 					  struct request_sock *req,
 					  struct dst_entry *dst,
 					  struct request_sock *req_unhash,
-					  bool *own_req,
-					  void (*opt_child_init)(struct sock *newsk,
-								 const struct sock *sk))
+					  bool *own_req)
 {
 	struct mptcp_subflow_context *listener = mptcp_subflow_ctx(sk);
 	struct mptcp_subflow_request_sock *subflow_req;
@@ -800,7 +798,7 @@ static struct sock *subflow_syn_recv_sock(const struct sock *sk,
 
 create_child:
 	child = listener->icsk_af_ops->syn_recv_sock(sk, skb, req, dst,
-						     req_unhash, own_req, opt_child_init);
+						     req_unhash, own_req);
 
 	if (child && *own_req) {
 		struct mptcp_subflow_context *ctx = mptcp_subflow_ctx(child);
--- a/net/smc/af_smc.c
+++ b/net/smc/af_smc.c
@@ -125,9 +125,7 @@ static struct sock *smc_tcp_syn_recv_sock(const struct sock *sk,
 					  struct request_sock *req,
 					  struct dst_entry *dst,
 					  struct request_sock *req_unhash,
-					  bool *own_req,
-					  void (*opt_child_init)(struct sock *newsk,
-								 const struct sock *sk))
+					  bool *own_req)
 {
 	struct smc_sock *smc;
 	struct sock *child;
@@ -152,7 +150,7 @@ static struct sock *smc_tcp_syn_recv_sock(const struct sock *sk,
 
 	/* passthrough to original syn recv sock fct */
 	child = smc->ori_af_ops->syn_recv_sock(sk, skb, req, dst, req_unhash,
-					       own_req, opt_child_init);
+					       own_req);
 	/* child must not inherit smc or its ops */
 	if (child) {
 		rcu_assign_sk_user_data(child, NULL);
