From: Takashi Iwai <tiwai@suse.de>
Subject: kABI workaround for struct auto_pin_cfg_item change
Patch-mainline: Never, kABI workaround
References: git-fixes

The patch
  patches.suse/ALSA-hda-Fix-headset-detection-failure-due-to-unstab.patch
introduced a new field into struct auto_pin_cfg_item.
Fold it into the existing bitfield for keeping kABI.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 sound/pci/hda/hda_auto_parser.h |    4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

--- a/sound/pci/hda/hda_auto_parser.h
+++ b/sound/pci/hda/hda_auto_parser.h
@@ -37,7 +37,9 @@ struct auto_pin_cfg_item {
 	unsigned int is_headset_mic:1;
 	unsigned int is_headphone_mic:1; /* Mic-only in headphone jack */
 	unsigned int has_boost_on_pin:1;
-	int order;
+#ifndef __GENKSYMS__
+	unsigned int order:8;
+#endif
 };
 
 struct auto_pin_cfg;
