From: Takashi Iwai <tiwai@suse.de>
Subject: kABI workaround for commit bbf9ac34677b backport
Patch-mainline: Never, kABI workaround
References: bsc#1225745, CVE-2024-36911, bsc#1229040

Rename to SUSE-specific one for kABI workaround.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 drivers/net/hyperv/netvsc.c |    4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

--- a/drivers/net/hyperv/netvsc.c
+++ b/drivers/net/hyperv/netvsc.c
@@ -155,9 +155,9 @@ static void free_netvsc_device(struct rc
 
 	kfree(nvdev->extension);
 
-	if (!nvdev->recv_buf_gpadl_handle.decrypted)
+	if (!SUSE_vmbus_gpadl_get_decrypted(&nvdev->recv_buf_gpadl_handle))
 		vfree(nvdev->recv_buf);
-	if (!nvdev->send_buf_gpadl_handle.decrypted)
+	if (!SUSE_vmbus_gpadl_get_decrypted(&nvdev->send_buf_gpadl_handle))
 		vfree(nvdev->send_buf);
 	bitmap_free(nvdev->send_section_map);
 
