From: Takashi Iwai <tiwai@suse.de>
Subject: kABI compatibility fix for max98373_priv struct
Patch-mainline: Never, kABI compat patch
References: git-fixes

The patch
  patches.suse/ASoC-max98373-sdw-use-first_hw_init-flag-on-resume.patch
renamed the exported struct max98373_priv.  Since it's a simple rename,
keep the compatiblity with the standard ifdef __GENKSYMS__.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 sound/soc/codecs/max98373.h |    9 ++++++++-
 1 file changed, 8 insertions(+), 1 deletion(-)

--- a/sound/soc/codecs/max98373.h
+++ b/sound/soc/codecs/max98373.h
@@ -223,7 +223,14 @@ struct max98373_priv {
 	/* variables to support soundwire */
 	struct sdw_slave *slave;
 	bool hw_init;
-	bool first_hw_init;
+#ifdef __GENKSYMS__
+	bool pm_init_once;
+#else
+	union {
+		bool first_hw_init;
+		bool pm_init_once;
+	};
+#endif
 	int slot;
 	unsigned int rx_mask;
 };
