From: Gabriel Krisman Bertazi <krisman@suse.de>
Subject: io_uring: kabi removepreserve io_buffer_list in io_ring_ctx
Patch-mainline: Never, kabi
References: git-fixes

This field went away in commit 09ab7eff3820 ("io_uring/kbuf: get rid of
lower BGID lists"). Preserve the kabi, but considering it is internal to
io_uring, turn it into a padding field.

---
 include/linux/io_uring_types.h |   10 ++++++++++
 1 file changed, 10 insertions(+)

--- a/include/linux/io_uring_types.h
+++ b/include/linux/io_uring_types.h
@@ -250,6 +250,16 @@ struct io_ring_ctx {
 
 		struct io_submit_state	submit_state;
 
+		/*
+		 * The following is internal to io_uring so it is safe
+                 * to turn it into pad space.
+		 */
+		#ifdef __GENKSYMS__
+			struct io_buffer_list   *io_bl;
+		#else
+			void			*kabi_pad1;
+		#endif
+
 		struct xarray		io_bl_xa;
 
 		struct io_hash_table	cancel_table_locked;
