From: Fernando Fernandez Mancera <fmancera@suse.de>
Date: Fri Sep 5 12:58:11 2025 +0200
Subject: kABI: adjust new field on ip_ct_sctp struct
References: git-fixes
Patch-mainline: Never, kABI workaround

Move the new "init" field at the bottom of the struct as there isn't any hole
available.

Signed-off-by: Fernando Fernandez Mancera <fmancera@suse.de>

---
 include/linux/netfilter/nf_conntrack_sctp.h |    4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

--- a/include/linux/netfilter/nf_conntrack_sctp.h
+++ b/include/linux/netfilter/nf_conntrack_sctp.h
@@ -9,9 +9,11 @@ struct ip_ct_sctp {
 	enum sctp_conntrack state;
 
 	__be32 vtag[IP_CT_DIR_MAX];
-	u8 init[IP_CT_DIR_MAX];
 	u8 last_dir;
 	u8 flags;
+#ifndef __GENKSYMS__
+	u8 init[IP_CT_DIR_MAX];
+#endif
 };
 
 #endif /* _NF_CONNTRACK_SCTP_H */
