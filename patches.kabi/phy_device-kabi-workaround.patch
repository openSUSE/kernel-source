From: Takashi Iwai <tiwai@suse.de>
Subject: struct phy_device kABI compatibility workaround
Patch-mainline: Never, kABI workaround
References: git-fixes

The patch 
  patches.suse/net-phy-fix-MDIO-bus-PM-PHY-resuming.patch
introduced a new bit field, suspended_by_mdio_bus, to struct
phy_device.
Move to the empty space for keeping the kABI compatbitility.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 include/linux/phy.h |    4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

--- a/include/linux/phy.h
+++ b/include/linux/phy.h
@@ -377,7 +377,6 @@ struct phy_device {
 	unsigned is_gigabit_capable:1;
 	unsigned has_fixups:1;
 	unsigned suspended:1;
-	unsigned suspended_by_mdio_bus:1;
 	unsigned sysfs_links:1;
 	unsigned loopback_enabled:1;
 
@@ -388,6 +387,9 @@ struct phy_device {
 
 	/* Interrupts are enabled */
 	unsigned interrupts:1;
+#ifndef __GENKSYMS__
+	unsigned suspended_by_mdio_bus:1;
+#endif
 
 	enum phy_state state;
 
