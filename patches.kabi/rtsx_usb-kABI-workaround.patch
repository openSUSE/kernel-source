From: Takashi Iwai <tiwai@suse.de>
Subject: kABI workaround for rtsx_usb
Patch-mainline: Never, kABI workaround
References: git-fixes

The recent fixes for rtsx-usb dropped a couple of fields in struct
rtsx_ucr that is exported.  Revive them just for kABI compatibility.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 include/linux/rtsx_usb.h |    2 ++
 1 file changed, 2 insertions(+)

--- a/include/linux/rtsx_usb.h
+++ b/include/linux/rtsx_usb.h
@@ -54,6 +54,8 @@ struct rtsx_ucr {
 	struct usb_device	*pusb_dev;
 	struct usb_interface	*pusb_intf;
 	struct usb_sg_request	current_sg;
+	unsigned char		*iobuf;		/* FIXME: SLE15-SP4 kABI placeholder */
+	dma_addr_t		iobuf_dma;	/* FIXME: SLE15-SP4 kABI placeholder */
 
 	struct timer_list	sg_timer;
 	struct mutex		dev_mutex;
