# This file contains upstream commit-ids that don't need to be backported to
# this kernel branch. Only one commit-id per line is allowed, and the full
# commit-id is required. Comments and empty lines are ignored.
