From: Pablo Neira Ayuso <pablo@netfilter.org>
Date: Tue, 31 Mar 2026 23:08:02 +0200
Subject: netfilter: nf_tables: reject immediate NF_QUEUE verdict
Git-commit: da107398cbd4bbdb6bffecb2ce86d5c9384f4cec
Patch-mainline: v6.18 or v6.18-rc8 (next release)
References: CVE-2026-43024 bsc#1263930

nft_queue is always used from userspace nftables to deliver the NF_QUEUE
verdict. Immediately emitting an NF_QUEUE verdict is never used by the
userspace nft tools, so reject immediate NF_QUEUE verdicts.

The arp family does not provide queue support, but such an immediate
verdict is still reachable. Globally reject NF_QUEUE immediate verdicts
to address this issue.

Fixes: f342de4e2f33 ("netfilter: nf_tables: reject QUEUE/DROP verdict parameters")
Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
Acked-by: Fernando Fernandez Mancera <fmancera@suse.de>
---
 net/netfilter/nf_tables_api.c |    7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

--- a/net/netfilter/nf_tables_api.c
+++ b/net/netfilter/nf_tables_api.c
@@ -5520,8 +5520,6 @@ static int nft_verdict_init(const struct
 	switch (data->verdict.code) {
 	case NF_ACCEPT:
 	case NF_DROP:
-	case NF_QUEUE:
-		break;
 	case NFT_CONTINUE:
 	case NFT_BREAK:
 	case NFT_RETURN:
@@ -5540,6 +5538,11 @@ static int nft_verdict_init(const struct
 		chain->use++;
 		data->verdict.chain = chain;
 		break;
+	case NF_QUEUE:
+		/* The nft_queue expression is used for this purpose, an
+		 * immediate NF_QUEUE verdict should not ever be seen here.
+		 * This was a fallthrough.
+		 */
 	default:
 		return -EINVAL;
 	}
