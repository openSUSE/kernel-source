dm-mpath.c:  Remove blk_run_queues() after resubmitting failed ios.
--- diff/drivers/md/dm-mpath.c	2004-03-16 11:47:14.322582568 +0000
+++ source/drivers/md/dm-mpath.c	2004-03-16 11:47:16.661227040 +0000
@@ -264,12 +264,6 @@ static void dispatch_failed_ios(void *da
 		generic_make_request(bio);
 		bio = next;
 	}
-
-	/*
-	 * FIXME: this now gets called once for each mpath
-	 * target, rather than once per daemon cycle.
-	 */
- 	blk_run_queues();
 }
 
 static void trigger_event(void *data)
