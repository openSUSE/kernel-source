Make sure to kick pdflush when we're having a hard time filling GFP_NOFS
allocations

Index: linux.t/mm/vmscan.c
===================================================================
--- linux.t.orig/mm/vmscan.c	2004-06-02 09:05:38.000000000 -0400
+++ linux.t/mm/vmscan.c	2004-06-02 09:09:55.000000000 -0400
@@ -876,8 +876,6 @@ int try_to_free_pages(struct zone **zone
 			ret = 1;
 			goto out;
 		}
-		if (!(gfp_mask & __GFP_FS))
-			break;		/* Let the caller handle it */
 		/*
 		 * Try to write back as many pages as we just scanned.  Not
 		 * sure if that makes sense, but it's an attempt to avoid
