From: jbeulich@novell.com
Subject: prevent re-generation of compile.h if only timestamp changed
Patch-mainline: never

--- /home/jbeulich/src/SuSE/kernel/head-2006-01-25/scripts/mkcompile_h	2006-01-25 09:52:23.000000000 +0100
+++ head-2006-01-25/scripts/mkcompile_h	2006-01-27 10:19:51.000000000 +0100
@@ -70,9 +70,11 @@ LINUX_COMPILE_VERSION_ID="__linux_compil
 # changed comment in the
 # first line.
 
+grep='grep -v UTS_VERSION\|LINUX_COMPILE_\(TIME\|VERSION_ID\)'
+
 if [ -r $TARGET ] && \
-      grep -v 'UTS_VERSION\|LINUX_COMPILE_TIME' $TARGET > .tmpver.1 && \
-      grep -v 'UTS_VERSION\|LINUX_COMPILE_TIME' .tmpcompile > .tmpver.2 && \
+      $grep $TARGET > .tmpver.1 && \
+      $grep .tmpcompile > .tmpver.2 && \
       cmp -s .tmpver.1 .tmpver.2; then
    rm -f .tmpcompile
 else
