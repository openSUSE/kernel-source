This patch fixes a refcounting bug introduced by the MIPv6 changes.

SuSE bug #42342

Index: linux-2.6.5/net/ipv6/tcp_ipv6.c
===================================================================
--- linux-2.6.5.orig/net/ipv6/tcp_ipv6.c	2004-06-24 15:18:13.000000000 +0200
+++ linux-2.6.5/net/ipv6/tcp_ipv6.c	2004-06-24 15:22:09.000000000 +0200
@@ -680,6 +680,8 @@
 #ifdef CONFIG_IPV6_SUBTREES
 // KK : Whole code looks like null stmt, dst_lookup fixed this for us.
                 ipv6_addr_copy(&fl.fl6_src, saddr);
+
+		dst_release(dst);
                 dst = ip6_route_output(sk, &fl);
 
                 if ((err = dst->error) != 0) {
