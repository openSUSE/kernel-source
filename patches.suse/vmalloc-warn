diff -u linux/mm/vmalloc.c-o linux/mm/vmalloc.c
--- linux/mm/vmalloc.c-o	2004-03-21 21:12:13.000000000 +0100
+++ linux/mm/vmalloc.c	2004-05-26 13:39:49.000000000 +0200
@@ -284,6 +284,7 @@
 
 	if ((PAGE_SIZE-1) & (unsigned long)addr) {
 		printk(KERN_ERR "Trying to vfree() bad address (%p)\n", addr);
+		WARN_ON(1);
 		return;
 	}
 
@@ -291,6 +292,7 @@
 	if (unlikely(!area)) {
 		printk(KERN_ERR "Trying to vfree() nonexistent vm area (%p)\n",
 				addr);
+		WARN_ON(1);
 		return;
 	}
 	
