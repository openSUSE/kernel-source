Index: linux-2.6.5/net/core/neighbour.c
===================================================================
--- linux-2.6.5.orig/net/core/neighbour.c	2004-04-21 22:29:14.000000000 +0200
+++ linux-2.6.5/net/core/neighbour.c	2004-04-26 14:57:43.000000000 +0200
@@ -690,6 +690,7 @@
 		} else {
 			neigh->nud_state = NUD_STALE;
 			neigh_suspect(neigh);
+			goto out;
 		}
 	} else if (state & NUD_DELAY) {
 		if (now - neigh->confirmed <= neigh->parms->delay_probe_time) {
