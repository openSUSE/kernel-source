From: okir@suse.de
Subject: Disable noisy printk in ICMPv6 code

This message can be triggered when doing PMTU over various
tunnel setups, including IPsec, and would clutter syslog

Index: linux-2.6.5/net/ipv6/ip6_output.c
===================================================================
--- linux-2.6.5.orig/net/ipv6/ip6_output.c	2004-05-07 13:55:09.000000000 +0200
+++ linux-2.6.5/net/ipv6/ip6_output.c	2004-05-07 13:56:35.000000000 +0200
@@ -276,8 +276,6 @@
 		return retval; 
 	}
 
-	if (net_ratelimit())
-		printk(KERN_DEBUG "IPv6: sending pkt_too_big to self\n");
 	skb->dev = dst->dev;
 	icmpv6_send(skb, ICMPV6_PKT_TOOBIG, 0, mtu, skb->dev);
 	kfree_skb(skb);
