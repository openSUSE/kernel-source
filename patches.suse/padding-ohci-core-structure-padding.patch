From 605e6f8b346423b8e48fe4c187014ed986a45f9d Mon Sep 17 00:00:00 2001
From: Oliver Neukum <oneukum@suse.com>
Date: Wed, 28 Feb 2024 12:57:14 +0100
Subject: [PATCH 3/5] padding: ohci core structure padding
Patch-mainline: Never, kabi padding
References: bsc#1220369

Just because it is old hardware does not mean that it won't
need fixes breaking kABI otherwise.

Signed-off-by: Oliver Neukum <oneukum@suse.com>
---
 drivers/usb/host/ohci.h | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/drivers/usb/host/ohci.h b/drivers/usb/host/ohci.h
index aac6285b3..83c7ae52a 100644
--- a/drivers/usb/host/ohci.h
+++ b/drivers/usb/host/ohci.h
@@ -74,6 +74,7 @@ struct ed {
 	struct td		*pending_td;
 #define	OKAY_TO_TAKEBACK(ohci, ed)			\
 		((int) (ohci->wdh_cnt - ed->takeback_wdh_cnt) >= 0)
+	void *suse_kabi_padding;
 
 } __attribute__ ((aligned(16)));
 
@@ -337,6 +338,7 @@ typedef struct urb_priv {
 	u16			length;		// # tds in this request
 	u16			td_cnt;		// tds already serviced
 	struct list_head	pending;
+	void *suse_kabi_padding;
 	struct td		*td[];		// all TDs in this request
 
 } urb_priv_t;
@@ -433,6 +435,7 @@ struct ohci_hcd {
 	struct work_struct	nec_work;	/* Worker for NEC quirk */
 
 	struct dentry		*debug_dir;
+	void *suse_kabi_padding;
 
 	/* platform-specific data -- must come last */
 	unsigned long           priv[] __aligned(sizeof(s64));
-- 
2.43.2

