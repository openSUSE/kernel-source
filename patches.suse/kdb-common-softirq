diff -purN linux-2.5/kernel/softirq.c linuxppc64-2.5/kernel/softirq.c
--- linux-2.5/kernel/softirq.c	2004-03-17 05:20:37.000000000 +0000
+++ linuxppc64-2.5/kernel/softirq.c	2004-03-20 11:56:19.000000000 +0000
@@ -16,6 +16,9 @@
 #include <linux/cpu.h>
 #include <linux/kthread.h>
 
+#ifdef CONFIG_KDB
+#include <linux/kdb.h>
+#endif
 /*
    - No shared variables, all the data are CPU local.
    - If a softirq needs serialization, let it serialize itself
@@ -77,6 +80,11 @@ asmlinkage void do_softirq(void)
 
 	if (in_interrupt())
 		return;
+#ifdef	CONFIG_KDB
+	if (KDB_IS_RUNNING())
+	    return;
+#endif	/*CONFIG_KDB */
+
 
 	local_irq_save(flags);
 
