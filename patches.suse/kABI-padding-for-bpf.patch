From: Shung-Hsi Yu <shung-hsi.yu@suse.com>
Date: Tue, 01 Fed 2022 08:43:36 +0800
Subject: [PATCH] kABI padding for bpf
Patch-mainline: Never, kABI padding
References: bsc#1179531

Add kABI padding to important BPF types

Signed-off-by: Shung-Hsi Yu <shung-hsi.yu@suse.com>
---
 include/linux/bpf.h          |    3 +++
 include/linux/bpf_verifier.h |    2 ++
 2 files changed, 5 insertions(+)

--- a/include/linux/bpf.h
+++ b/include/linux/bpf.h
@@ -332,6 +332,7 @@ struct bpf_map {
 	atomic64_t sleepable_refcnt;
 	s64 __percpu *elem_count;
 	u64 cookie; /* write-once */
+	void *suse_kabi_padding;
 };
 
 static inline const char *btf_field_type_name(enum btf_field_type type)
@@ -1698,6 +1699,7 @@ struct bpf_prog_aux {
 		struct rcu_head	rcu;
 	};
 	struct bpf_stream stream[2];
+	void *suse_kabi_padding;
 };
 
 struct bpf_prog {
@@ -1733,6 +1735,7 @@ struct bpf_prog {
 		DECLARE_FLEX_ARRAY(struct sock_filter, insns);
 		DECLARE_FLEX_ARRAY(struct bpf_insn, insnsi);
 	};
+	void *suse_kabi_padding;
 };
 
 struct bpf_array_aux {
--- a/include/linux/bpf_verifier.h
+++ b/include/linux/bpf_verifier.h
@@ -463,6 +463,7 @@ struct bpf_verifier_state {
 	u32 dfs_depth;
 	u32 callback_unroll_depth;
 	u32 may_goto_depth;
+	void *suse_kabi_padding;
 };
 
 #define bpf_get_spilled_reg(slot, frame, mask)				\
@@ -853,6 +854,7 @@ struct bpf_verifier_env {
 	struct bpf_scc_info **scc_info;
 	u32 scc_cnt;
 	bool internal_error;
+	void *suse_kabi_padding;
 };
 
 static inline struct bpf_func_info_aux *subprog_aux(struct bpf_verifier_env *env, int subprog)
