From: Farhan Ali <alifm@linux.ibm.com>
Subject: KVM: s390: pci: Fix NULL dereference on AIBV allocation failure
Patch-mainline: v7.2-rc6
Git-commit: 8bf09b9b7d3232806df95f409581f8a9fd99a3fa
References: bsc#1277451

Description: KVM/s390/pci: Fix resource leaks and cleanup in error paths
Symptom: resource leaks, denial-of-service
Problem: Error paths in interrupt forwarding logic for passthrough PCI devices
         failed to cleanup resources correctly.       
Solution: Fix the logic and handle cleanup appropriately.      
Reproduction:  -

Upstream-Description:

              KVM: s390: pci: Fix NULL dereference on AIBV allocation failure

              The airq_iv_create() can return NULL on failure, but the return value was
              never checked. If it fails, zdev->aibv will be NULL and fail when
              dereferenced in kvm_zpci_set_airq(). Add a NULL check and free the
              previously allocated AISB bit and zdev->aisb on failure.

              Fixes: 3c5a1b6f0a18 ("KVM: s390: pci: provide routines for enabling/disabling interrupt forwarding")
              Cc: stable@vger.kernel.org
              Reviewed-by: Christian Borntraeger <borntraeger@linux.ibm.com>
              Reviewed-by: Matthew Rosato <mjrosato@linux.ibm.com>
              Signed-off-by: Farhan Ali <alifm@linux.ibm.com>
              Tested-by: Matthew Rosato <mjrosato@linux.ibm.com>
              Signed-off-by: Christian Borntraeger <borntraeger@linux.ibm.com>


Signed-off-by: Farhan Ali <alifm@linux.ibm.com>
Acked-by: Hannes Reinecke <hare@suse.de>
---
 arch/s390/kvm/pci.c |    8 ++++++++
 1 file changed, 8 insertions(+)

--- a/arch/s390/kvm/pci.c
+++ b/arch/s390/kvm/pci.c
@@ -319,6 +319,11 @@ static int kvm_s390_pci_aif_enable(struc
 				    AIRQ_IV_GUESTVEC,
 				    phys_to_virt(fib->fmt0.aibv));
 
+	if (!zdev->aibv) {
+		rc = -ENOMEM;
+		goto free_aisb;
+	}
+
 	spin_lock_irq(&aift->gait_lock);
 	gaite = aift->gait + zdev->aisb;
 
@@ -355,6 +360,9 @@ static int kvm_s390_pci_aif_enable(struc
 	rc = kvm_zpci_set_airq(zdev);
 	return rc;
 
+free_aisb:
+	airq_iv_free_bit(aift->sbv, zdev->aisb);
+	zdev->aisb = 0;
 unlock:
 	if (pcount > 0)
 		unaccount_mem(zdev->kzdev, pcount);
