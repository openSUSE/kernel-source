--- linux-2.6.8/kdb/modules/kdbm_pg.c.~1~	2004-09-16 17:37:34.570863903 +0200
+++ linux-2.6.8/kdb/modules/kdbm_pg.c	2004-09-16 17:39:51.532679454 +0200
@@ -281,8 +281,6 @@ kdbm_show_page(struct page *page, int fi
 	kdb_page_flags(page, Private);
 	kdb_page_flags(page, Writeback);
 	kdb_page_flags(page, Nosave);
-	if (page->flags & (1UL << PG_maplock))
-		kdb_printf(" maplock");
 	kdb_page_flags(page, SwapCache);
 	kdb_page_flags(page, MappedToDisk);
 	kdb_page_flags(page, Reclaim);
