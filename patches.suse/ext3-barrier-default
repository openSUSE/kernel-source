Hello everyone,

This patch turns on barriers by default for ext3.  mount -o barrier=0
will turn them off.  It also changes the ext3 fsync call to trigger a
barrier when a commit isn't done.

It should be safe, but some extra review would be appreciated.

Index: linux.t/fs/ext3/fsync.c
===================================================================
--- linux.t.orig/fs/ext3/fsync.c	2004-06-08 14:13:35.000000000 -0400
+++ linux.t/fs/ext3/fsync.c	2004-06-13 22:22:57.000000000 -0400
@@ -27,6 +27,7 @@
 #include <linux/sched.h>
 #include <linux/writeback.h>
 #include <linux/jbd.h>
+#include <linux/blkdev.h>
 #include <linux/ext3_fs.h>
 #include <linux/ext3_jbd.h>
 
@@ -85,7 +86,10 @@ int ext3_sync_file(struct file * file, s
 			.sync_mode = WB_SYNC_ALL,
 			.nr_to_write = 0, /* sys_fsync did this */
 		};
+		journal_t *journal = EXT3_SB(inode->i_sb)->s_journal;
 		ret = sync_inode(inode, &wbc);
+		if (journal && (journal->j_flags & JFS_BARRIER))
+			blkdev_issue_flush(inode->i_sb->s_bdev, NULL);
 	}
 out:
 	return ret;
Index: linux.t/fs/ext3/super.c
===================================================================
--- linux.t.orig/fs/ext3/super.c	2004-06-08 14:14:36.000000000 -0400
+++ linux.t/fs/ext3/super.c	2004-06-13 22:32:04.318185776 -0400
@@ -1130,6 +1130,9 @@ static int ext3_fill_super (struct super
 	sbi->s_resuid = le16_to_cpu(es->s_def_resuid);
 	sbi->s_resgid = le16_to_cpu(es->s_def_resgid);
 
+	/* enable barriers by default */
+	set_opt(sbi->s_mount_opt, BARRIER);
+
 	if (!parse_options ((char *) data, sbi, &journal_inum, 0))
 		goto failed_mount;
 


