dm-raid1.c: Remove blk_run_queues().
--- diff/drivers/md/dm-raid1.c	2004-03-16 11:47:42.563289328 +0000
+++ source/drivers/md/dm-raid1.c	2004-03-16 11:47:46.889631624 +0000
@@ -859,7 +859,6 @@ static void do_mirror(struct mirror_set 
 	do_recovery(ms);
 	do_reads(ms, &reads);
 	do_writes(ms, &writes);
-	blk_run_queues();
 }
 
 static void do_work(void *ignored)
