From: Hannes Reinecke <hare@suse.de>
Date: Wed, 15 Jan 2020 14:40:02 +0100
Subject: [PATCH] acpi: disable deprecated warnings
References: bsc#1160977
Patch-Mainline: never, SLES-specific

While the interface is deprecated we cannot disable it for SLES
as we need to keep backwards compability.
So just disable the warning.

Signed-off-by: Hannes Reinecke <hare@suse.de>
---
 drivers/acpi/ac.c      | 2 ++
 drivers/acpi/battery.c | 2 ++
 2 files changed, 4 insertions(+)

diff --git a/drivers/acpi/ac.c b/drivers/acpi/ac.c
index 829f37d36b9f..d7bbb0eb6710 100644
--- a/drivers/acpi/ac.c
+++ b/drivers/acpi/ac.c
@@ -190,8 +190,10 @@ static int acpi_ac_add_fs(struct acpi_ac *ac)
 {
 	struct proc_dir_entry *entry = NULL;
 
+#if 0
 	printk(KERN_WARNING PREFIX "Deprecated procfs I/F for AC is loaded,"
 			" please retry with CONFIG_ACPI_PROCFS_POWER cleared\n");
+#endif
 	if (!acpi_device_dir(ac->device)) {
 		acpi_device_dir(ac->device) =
 			proc_mkdir(acpi_device_bid(ac->device), acpi_ac_dir);
diff --git a/drivers/acpi/battery.c b/drivers/acpi/battery.c
index 558fedf8a7a1..50677c733051 100644
--- a/drivers/acpi/battery.c
+++ b/drivers/acpi/battery.c
@@ -1176,7 +1176,9 @@ static const struct file_operations acpi_battery_alarm_fops = {
 
 static int acpi_battery_add_fs(struct acpi_device *device)
 {
+#if 0
 	pr_warning(PREFIX "Deprecated procfs I/F for battery is loaded, please retry with CONFIG_ACPI_PROCFS_POWER cleared\n");
+#endif
 	if (!acpi_device_dir(device)) {
 		acpi_device_dir(device) = proc_mkdir(acpi_device_bid(device),
 						     acpi_battery_dir);
-- 
2.16.4

