--- temp/linux-2.6.5-7.61/arch/ppc64/kdb/kdbasupport.c	2004-05-27 22:59:51.000000000 -0700
+++ linux-2.6.5-7.61/arch/ppc64/kdb/kdbasupport.c	2004-06-07 01:20:48.083260352 -0700
@@ -1784,6 +1784,12 @@ kdb_debugger(struct pt_regs *regs) {
 	if (regs) {
 		if (regs->trap==0x100) {
 			kdb_reset_debugger(regs);
+			if (!(regs->msr & MSR_RI)) {
+				/* unrecoverable exception, spin forever, 
+				 * don't return to exception handler
+				 */
+				while (1) {}
+			}	
 		} else {
 			kdb(KDB_REASON_ENTER,regs->trap,regs);   /* ok */
 		}
