From: ak@suse.de
Subject: Add KDEBUG events needed for KDB

It's actually not needed for !IA64, but let's just add it here
instead of doing ifdefs,

Index: linux-2.6.15/include/asm-x86_64/kdebug.h
===================================================================
--- linux-2.6.15.orig/include/asm-x86_64/kdebug.h
+++ linux-2.6.15/include/asm-x86_64/kdebug.h
@@ -33,6 +33,8 @@ enum die_val { 
 	DIE_CALL,
 	DIE_NMI_IPI,
 	DIE_PAGE_FAULT,
+	DIE_KDEBUG_ENTER,
+	DIE_KDEBUG_LEAVE,
 }; 
 	
 static inline int notify_die(enum die_val val, const char *str,
