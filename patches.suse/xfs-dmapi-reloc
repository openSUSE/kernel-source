Index: linux-2.6.2/fs/xfs/Makefile
===================================================================
--- linux-2.6.2.orig/fs/xfs/Makefile
+++ linux-2.6.2/fs/xfs/Makefile
@@ -30,9 +30,9 @@
 # http://oss.sgi.com/projects/GenInfo/SGIGPLNoticeExplan/
 #
 
-EXTRA_CFLAGS +=	 -Ifs/xfs -Ifs/xfs/linux-2.6 -funsigned-char
+EXTRA_CFLAGS +=	 -Ifs/xfs -Ifs/xfs/linux -funsigned-char
 
-XFS_LINUX := linux-2.6
+XFS_LINUX := linux
 
 ifeq ($(CONFIG_XFS_DEBUG),y)
 	EXTRA_CFLAGS += -g -DSTATIC="" -DDEBUG -DXFSDEBUG
Index: linux-2.6.2/fs/xfs/dmapi/Makefile
===================================================================
--- linux-2.6.2.orig/fs/xfs/dmapi/Makefile
+++ linux-2.6.2/fs/xfs/dmapi/Makefile
@@ -30,7 +30,7 @@
 # http://oss.sgi.com/projects/GenInfo/SGIGPLNoticeExplan/
 #
 
-EXTRA_CFLAGS += -I $(TOPDIR)/fs/xfs -I $(TOPDIR)/fs/xfs/linux-2.6
+EXTRA_CFLAGS += -I $(TOPDIR)/fs/xfs -I $(TOPDIR)/fs/xfs/linux
 
 ifeq ($(CONFIG_XFS_DEBUG),y)
 	EXTRA_CFLAGS += -g -DDEBUG -DXFSDEBUG
Index: linux-2.6.2/fs/xfs/quota/Makefile
===================================================================
--- linux-2.6.2.orig/fs/xfs/quota/Makefile
+++ linux-2.6.2/fs/xfs/quota/Makefile
@@ -30,7 +30,7 @@
 # http://oss.sgi.com/projects/GenInfo/SGIGPLNoticeExplan/
 #
 
-EXTRA_CFLAGS += -I $(TOPDIR)/fs/xfs -I $(TOPDIR)/fs/xfs/linux-2.6
+EXTRA_CFLAGS += -I $(TOPDIR)/fs/xfs -I $(TOPDIR)/fs/xfs/linux
 
 ifeq ($(CONFIG_XFS_DEBUG),y)
 	EXTRA_CFLAGS += -g -DDEBUG -DXFSDEBUG
Index: linux-2.6.2/fs/xfs/xfs.h
===================================================================
--- linux-2.6.2.orig/fs/xfs/xfs.h
+++ linux-2.6.2/fs/xfs/xfs.h
@@ -52,8 +52,8 @@
 #include <support/time.h>
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(2,6,0)
-#include <linux-2.6/kmem.h>
-#include <linux-2.6/xfs_linux.h>
+#include <linux/kmem.h>
+#include <linux/xfs_linux.h>
 #else
 #include <linux-2.4/kmem.h>
 #include <linux-2.4/xfs_linux.h>
