From: Ivan Vecera <ivecera@redhat.com>
Date: Fri, 4 Jul 2025 20:21:53 +0200
Subject: devlink: Add support for u64 parameters
Patch-mainline: v6.17-rc1
Git-commit: c0ef1446959101d23fdf1b1bdefc6613a83dba03
References: jsc#PED-12745

Only 8, 16 and 32-bit integers are supported for numeric devlink
parameters. The subsequent patch adds support for DPLL clock ID
that is defined as 64-bit number. Add support for u64 parameter
type.

Signed-off-by: Ivan Vecera <ivecera@redhat.com>
Reviewed-by: Jiri Pirko <jiri@nvidia.com>
Link: https://patch.msgid.link/20250704182202.1641943-4-ivecera@redhat.com
Signed-off-by: Jakub Kicinski <kuba@kernel.org>
Acked-by: Thomas Bogendoerfer <tbogendoerfer@suse.de>
---
 include/net/devlink.h |    2 ++
 net/devlink/param.c   |   15 +++++++++++++++
 2 files changed, 17 insertions(+)

--- a/include/net/devlink.h
+++ b/include/net/devlink.h
@@ -424,12 +424,14 @@ enum devlink_param_type {
 	DEVLINK_PARAM_TYPE_U32,
 	DEVLINK_PARAM_TYPE_STRING,
 	DEVLINK_PARAM_TYPE_BOOL,
+	DEVLINK_PARAM_TYPE_U64
 };
 
 union devlink_param_value {
 	u8 vu8;
 	u16 vu16;
 	u32 vu32;
+	u64 vu64;
 	char vstr[__DEVLINK_PARAM_MAX_STRING_VALUE];
 	bool vbool;
 };
--- a/net/devlink/param.c
+++ b/net/devlink/param.c
@@ -176,6 +176,8 @@ devlink_param_type_to_var_attr_type(enum
 		return DEVLINK_VAR_ATTR_TYPE_U16;
 	case DEVLINK_PARAM_TYPE_U32:
 		return DEVLINK_VAR_ATTR_TYPE_U32;
+	case DEVLINK_PARAM_TYPE_U64:
+		return DEVLINK_VAR_ATTR_TYPE_U64;
 	case DEVLINK_PARAM_TYPE_STRING:
 		return DEVLINK_VAR_ATTR_TYPE_STRING;
 	case DEVLINK_PARAM_TYPE_BOOL:
@@ -214,6 +216,11 @@ devlink_nl_param_value_fill_one(struct s
 		if (nla_put_u32(msg, DEVLINK_ATTR_PARAM_VALUE_DATA, val.vu32))
 			goto value_nest_cancel;
 		break;
+	case DEVLINK_PARAM_TYPE_U64:
+		if (devlink_nl_put_u64(msg, DEVLINK_ATTR_PARAM_VALUE_DATA,
+				       val.vu64))
+			goto value_nest_cancel;
+		break;
 	case DEVLINK_PARAM_TYPE_STRING:
 		if (nla_put_string(msg, DEVLINK_ATTR_PARAM_VALUE_DATA,
 				   val.vstr))
@@ -429,6 +436,9 @@ devlink_param_type_get_from_info(struct
 	case DEVLINK_VAR_ATTR_TYPE_U32:
 		*param_type = DEVLINK_PARAM_TYPE_U32;
 		break;
+	case DEVLINK_VAR_ATTR_TYPE_U64:
+		*param_type = DEVLINK_PARAM_TYPE_U64;
+		break;
 	case DEVLINK_VAR_ATTR_TYPE_STRING:
 		*param_type = DEVLINK_PARAM_TYPE_STRING;
 		break;
@@ -471,6 +481,11 @@ devlink_param_value_get_from_info(const
 			return -EINVAL;
 		value->vu32 = nla_get_u32(param_data);
 		break;
+	case DEVLINK_PARAM_TYPE_U64:
+		if (nla_len(param_data) != sizeof(u64))
+			return -EINVAL;
+		value->vu64 = nla_get_u64(param_data);
+		break;
 	case DEVLINK_PARAM_TYPE_STRING:
 		len = strnlen(nla_data(param_data), nla_len(param_data));
 		if (len == nla_len(param_data) ||
