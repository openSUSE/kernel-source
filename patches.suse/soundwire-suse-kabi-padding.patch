From: Takashi Iwai <tiwai@suse.de>
Subject: kABI padding for soundwire
Patch-mainline: Never, kABI padding
References: kABI padding

Add a few kABI padding fields to soundwire-related structs.

Signed-off-by: Takashi Iwai <tiwai@suse.de>

---
 include/linux/soundwire/sdw.h |    3 +++
 1 file changed, 3 insertions(+)

--- a/include/linux/soundwire/sdw.h
+++ b/include/linux/soundwire/sdw.h
@@ -679,6 +679,7 @@ struct sdw_slave {
 	bool is_mockup_device;
 	struct mutex sdw_dev_lock; /* protect callbacks/remove races */
 	struct sdca_device_data sdca_data;
+	void *suse_kabi_padding;	/* XXX SLE-specific kABI placeholder */
 };
 
 #define dev_to_sdw_dev(_dev) container_of(_dev, struct sdw_slave, dev)
@@ -942,6 +943,7 @@ struct sdw_stream_runtime {
 	enum sdw_stream_type type;
 	int m_rt_count;
 	struct list_head master_list;
+	void *suse_kabi_padding;	/* XXX SLE-specific kABI placeholder */
 };
 
 /**
@@ -1015,6 +1017,7 @@ struct sdw_bus {
 #endif
 	bool multi_link;
 	unsigned int lane_used_bandwidth[SDW_MAX_LANES];
+	void *suse_kabi_padding;	/* XXX SLE-specific kABI placeholder */
 };
 
 struct sdw_stream_runtime *sdw_alloc_stream(const char *stream_name);
