From: Olaf Hering <olh@suse.de>
Subject: disable debug output
Patch-mainline: never

ratelimit is not enough, hide the whole message

 drivers/ieee1394/ohci1394.c |    2 ++
 1 files changed, 2 insertions(+)

Index: linux-2.6.13-ohci1394/drivers/ieee1394/ohci1394.c
===================================================================
--- linux-2.6.13-ohci1394.orig/drivers/ieee1394/ohci1394.c
+++ linux-2.6.13-ohci1394/drivers/ieee1394/ohci1394.c
@@ -1083,8 +1083,10 @@ static int ohci_devctl(struct hpsb_host 
 
 			initialize_dma_rcv_ctx(&ohci->ir_legacy_context, 1);
 
+#if 0
 			if (printk_ratelimit())
 				PRINT(KERN_ERR, "IR legacy activated");
+#endif
 		}
 
                 spin_lock_irqsave(&ohci->IR_channel_lock, flags);
