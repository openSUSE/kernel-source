From 379954b607cd3c094fced46fecac1d9096859666 Mon Sep 17 00:00:00 2001
From: Oliver Neukum <oneukum@suse.com>
Date: Tue, 24 Jan 2023 09:38:05 +0100
Subject: [PATCH 3/3] paddings for gadgets
Patch-mainline: Never (kABI padding)
References: bsc#1207439

Signed-off-by: Oliver Neukum <oneukum@suse.com>
---
 include/linux/usb/gadget.h |   12 ++++++++++++
 1 file changed, 12 insertions(+)

--- a/include/linux/usb/gadget.h
+++ b/include/linux/usb/gadget.h
@@ -123,6 +123,8 @@ struct usb_request {
 
 	int			status;
 	unsigned		actual;
+
+	void *suse_kabi_padding;
 };
 
 /*-------------------------------------------------------------------------*/
@@ -153,6 +155,8 @@ struct usb_ep_ops {
 
 	int (*fifo_status) (struct usb_ep *ep);
 	void (*fifo_flush) (struct usb_ep *ep);
+
+	void *suse_kabi_padding;
 };
 
 /**
@@ -241,6 +245,8 @@ struct usb_ep {
 	u16			maxpacket;
 	u16			maxpacket_limit;
 	u16			max_streams;
+
+	void *suse_kabi_padding;
 };
 
 /*-------------------------------------------------------------------------*/
@@ -333,6 +339,8 @@ struct usb_gadget_ops {
 			struct usb_endpoint_descriptor *,
 			struct usb_ss_ep_comp_descriptor *);
 	int	(*check_config)(struct usb_gadget *gadget);
+
+	void *suse_kabi_padding;
 };
 
 /**
@@ -455,6 +463,8 @@ struct usb_gadget {
 	unsigned			wakeup_armed:1;
 	int				irq;
 	int				id_number;
+
+	void *suse_kabi_padding;
 };
 #define work_to_gadget(w)	(container_of((w), struct usb_gadget, work))
 
@@ -752,6 +762,8 @@ struct usb_gadget_driver {
 	char			*udc_name;
 	unsigned                match_existing_only:1;
 	bool			is_bound:1;
+
+	void *suse_kabi_padding;
 };
 
 
