Using barriers by default on ext3 means that ext3 will generate some
scary end_io warnings on scsi drives.  These should be surpressed.

--- linux-2.6.7/fs/buffer.c~	2004-08-10 11:53:45.888470313 +0200
+++ linux-2.6.7/fs/buffer.c	2004-08-10 11:53:49.842045937 +0200
@@ -213,7 +213,7 @@
 	if (uptodate) {
 		set_buffer_uptodate(bh);
 	} else {
-		if (printk_ratelimit()) {
+		if (!buffer_eopnotsupp(bh) && printk_ratelimit()) {
 			buffer_io_error(bh);
 			printk(KERN_WARNING "lost page write due to "
 					"I/O error on %s\n",
