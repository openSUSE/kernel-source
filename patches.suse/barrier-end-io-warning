Using barriers by default on ext3 means that ext3 will generate some
scary end_io warnings on scsi drives.  These should be surpressed.

Index: linux.t/fs/buffer.c
===================================================================
--- linux.t.orig/fs/buffer.c	2004-06-21 10:50:29.000000000 -0400
+++ linux.t/fs/buffer.c	2004-06-22 11:36:59.000000000 -0400
@@ -197,7 +197,7 @@ void end_buffer_write_sync(struct buffer
 	if (uptodate) {
 		set_buffer_uptodate(bh);
 	} else {
-		if (printk_ratelimit()) {
+		if (!buffer_eopnotsupp(bh) && printk_ratelimit()) {
 			buffer_io_error(bh);
 			printk(KERN_WARNING "lost page write due to "
 					"I/O error on %s\n",
