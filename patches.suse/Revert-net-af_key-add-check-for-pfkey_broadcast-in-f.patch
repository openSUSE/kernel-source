From: Michal Kubecek <mkubecek@suse.cz>
Date: Mon, 23 May 2022 09:55:13 +0200
Subject: Revert "net: af_key: add check for pfkey_broadcast in function pfkey_process"
Patch-mainline: Not yet, under discussion
References: 20220523022438.ofhehjievu2alj3h@lion.mk-sys.cz

This reverts commit 4dc2a5a8f6754492180741facf2a8787f2c415d7.

This commit seems to break processing of AF_KEY messages, breaking racoon
daemon.

Signed-off-by: Michal Kubecek <mkubecek@suse.cz>
---
 net/key/af_key.c | 6 ++----
 1 file changed, 2 insertions(+), 4 deletions(-)

--- a/net/key/af_key.c
+++ b/net/key/af_key.c
@@ -2826,10 +2826,8 @@ static int pfkey_process(struct sock *sk, struct sk_buff *skb, const struct sadb
 	void *ext_hdrs[SADB_EXT_MAX];
 	int err;
 
-	err = pfkey_broadcast(skb_clone(skb, GFP_KERNEL), GFP_KERNEL,
-			      BROADCAST_PROMISC_ONLY, NULL, sock_net(sk));
-	if (err)
-		return err;
+	pfkey_broadcast(skb_clone(skb, GFP_KERNEL), GFP_KERNEL,
+			BROADCAST_PROMISC_ONLY, NULL, sock_net(sk));
 
 	memset(ext_hdrs, 0, sizeof(ext_hdrs));
 	err = parse_exthdrs(skb, hdr, ext_hdrs);
