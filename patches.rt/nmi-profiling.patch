Subject: Linux-RT 2.6.26-RT
From: http://www.kernel.org/pub/linux/kernel/projects/rt/
Acked-by: Sven-Thorsten Dietrich <sdietrich@suse.de>
---
 arch/x86/kernel/irq_32.c |    2 ++
 arch/x86/kernel/nmi_32.c |    5 ++---
 arch/x86/kernel/nmi_64.c |    4 ++--
 drivers/char/sysrq.c     |    2 +-
 4 files changed, 7 insertions(+), 6 deletions(-)

Index: linux-2.6.26/arch/x86/kernel/irq_32.c
===================================================================
--- linux-2.6.26.orig/arch/x86/kernel/irq_32.c
+++ linux-2.6.26/arch/x86/kernel/irq_32.c
@@ -79,7 +79,9 @@ unsigned int do_IRQ(struct pt_regs *regs
 	u32 *isp;
 #endif
 
+#ifdef CONFIG_X86_LOCAL_APIC
 	irq_show_regs_callback(smp_processor_id(), regs);
+#endif
 
 	if (unlikely((unsigned)irq >= NR_IRQS)) {
 		printk(KERN_EMERG "%s: cannot handle IRQ %d\n",
Index: linux-2.6.26/arch/x86/kernel/nmi_32.c
===================================================================
--- linux-2.6.26.orig/arch/x86/kernel/nmi_32.c
+++ linux-2.6.26/arch/x86/kernel/nmi_32.c
@@ -346,9 +346,9 @@ void nmi_show_all_regs(void)
 	}
 }
 
-static DEFINE_SPINLOCK(nmi_print_lock);
+static DEFINE_RAW_SPINLOCK(nmi_print_lock);
 
-void irq_show_regs_callback(int cpu, struct pt_regs *regs)
+notrace void irq_show_regs_callback(int cpu, struct pt_regs *regs)
 {
 	if (!nmi_show_regs[cpu])
 		return;
@@ -433,7 +433,6 @@ nmi_watchdog_tick(struct pt_regs *regs, 
 			for_each_online_cpu(i)
 				alert_counter[i] = 0;
 
-
 		}
 
 	} else {
Index: linux-2.6.26/arch/x86/kernel/nmi_64.c
===================================================================
--- linux-2.6.26.orig/arch/x86/kernel/nmi_64.c
+++ linux-2.6.26/arch/x86/kernel/nmi_64.c
@@ -337,9 +337,9 @@ void nmi_show_all_regs(void)
 	}
 }
 
-static DEFINE_SPINLOCK(nmi_print_lock);
+static DEFINE_RAW_SPINLOCK(nmi_print_lock);
 
-void irq_show_regs_callback(int cpu, struct pt_regs *regs)
+notrace void irq_show_regs_callback(int cpu, struct pt_regs *regs)
 {
 	if (!nmi_show_regs[cpu])
 		return;
Index: linux-2.6.26/drivers/char/sysrq.c
===================================================================
--- linux-2.6.26.orig/drivers/char/sysrq.c
+++ linux-2.6.26/drivers/char/sysrq.c
@@ -251,7 +251,7 @@ static struct sysrq_key_op sysrq_showreg
 	.enable_mask	= SYSRQ_ENABLE_DUMP,
 };
 
-#if defined(__i386__)
+#if defined(__i386__) || defined(__x86_64__)
 
 static void sysrq_handle_showallregs(int key, struct tty_struct *tty)
 {
