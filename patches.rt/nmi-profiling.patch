Subject: Linux-RT 2.6.24-rc8-rt1
From: http://www.kernel.org/pub/linux/kernel/projects/rt/
Acked-by: Sven-Thorsten Dietrich <sdietrich@suse.de>
---
 arch/x86/kernel/irq_32.c  |    2 ++
 arch/x86/kernel/nmi_32.c  |    5 ++---
 arch/x86/kernel/nmi_64.c  |    4 ++--
 drivers/char/sysrq.c      |    2 +-
 include/asm-x86/apic_64.h |    2 ++
 5 files changed, 9 insertions(+), 6 deletions(-)

Index: linux-2.6.24-rc8-rt1/arch/x86/kernel/irq_32.c
===================================================================
--- linux-2.6.24-rc8-rt1.orig/arch/x86/kernel/irq_32.c	2008-01-16 22:28:47.000000000 -0500
+++ linux-2.6.24-rc8-rt1/arch/x86/kernel/irq_32.c	2008-01-16 22:28:57.000000000 -0500
@@ -77,7 +77,9 @@ notrace fastcall unsigned int do_IRQ(str
 	u32 *isp;
 #endif
 
+#ifdef CONFIG_X86_LOCAL_APIC
 	irq_show_regs_callback(smp_processor_id(), regs);
+#endif
 
 	if (unlikely((unsigned)irq >= NR_IRQS)) {
 		printk(KERN_EMERG "%s: cannot handle IRQ %d\n",
Index: linux-2.6.24-rc8-rt1/arch/x86/kernel/nmi_32.c
===================================================================
--- linux-2.6.24-rc8-rt1.orig/arch/x86/kernel/nmi_32.c	2008-01-16 22:28:47.000000000 -0500
+++ linux-2.6.24-rc8-rt1/arch/x86/kernel/nmi_32.c	2008-01-16 22:28:57.000000000 -0500
@@ -354,9 +354,9 @@ void nmi_show_all_regs(void)
 	}
 }
 
-static DEFINE_SPINLOCK(nmi_print_lock);
+static DEFINE_RAW_SPINLOCK(nmi_print_lock);
 
-void irq_show_regs_callback(int cpu, struct pt_regs *regs)
+notrace void irq_show_regs_callback(int cpu, struct pt_regs *regs)
 {
 	if (!nmi_show_regs[cpu])
 		return;
@@ -440,7 +440,6 @@ notrace __kprobes int nmi_watchdog_tick(
 			for_each_online_cpu(i)
 				alert_counter[i] = 0;
 
-
 		}
 
 	} else {
Index: linux-2.6.24-rc8-rt1/arch/x86/kernel/nmi_64.c
===================================================================
--- linux-2.6.24-rc8-rt1.orig/arch/x86/kernel/nmi_64.c	2008-01-16 22:28:42.000000000 -0500
+++ linux-2.6.24-rc8-rt1/arch/x86/kernel/nmi_64.c	2008-01-16 22:28:57.000000000 -0500
@@ -339,9 +339,9 @@ void nmi_show_all_regs(void)
 	}
 }
 
-static DEFINE_SPINLOCK(nmi_print_lock);
+static DEFINE_RAW_SPINLOCK(nmi_print_lock);
 
-void irq_show_regs_callback(int cpu, struct pt_regs *regs)
+notrace void irq_show_regs_callback(int cpu, struct pt_regs *regs)
 {
 	if (!nmi_show_regs[cpu])
 		return;
Index: linux-2.6.24-rc8-rt1/drivers/char/sysrq.c
===================================================================
--- linux-2.6.24-rc8-rt1.orig/drivers/char/sysrq.c	2008-01-16 22:28:52.000000000 -0500
+++ linux-2.6.24-rc8-rt1/drivers/char/sysrq.c	2008-01-16 22:28:57.000000000 -0500
@@ -209,7 +209,7 @@ static struct sysrq_key_op sysrq_showreg
 	.enable_mask	= SYSRQ_ENABLE_DUMP,
 };
 
-#if defined(__i386__)
+#if defined(__i386__) || defined(__x86_64__)
 
 static void sysrq_handle_showallregs(int key, struct tty_struct *tty)
 {
Index: linux-2.6.24-rc8-rt1/include/asm-x86/apic_64.h
===================================================================
--- linux-2.6.24-rc8-rt1.orig/include/asm-x86/apic_64.h	2008-01-16 22:28:11.000000000 -0500
+++ linux-2.6.24-rc8-rt1/include/asm-x86/apic_64.h	2008-01-16 22:28:57.000000000 -0500
@@ -96,6 +96,8 @@ extern void smp_send_nmi_allbutself(void
 #define K8_APIC_EXT_INT_MSG_EXT 0x7
 #define K8_APIC_EXT_LVT_ENTRY_THRESHOLD    0
 
+extern void smp_send_nmi_allbutself(void);
+
 #define ARCH_APICTIMER_STOPS_ON_C3	1
 
 extern unsigned boot_cpu_id;
