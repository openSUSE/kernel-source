Subject: Linux-RT 2.6.24-rc2-rt1
From: http://www.kernel.org/pub/linux/kernel/projects/rt/
Acked-by: Sven-Thorsten Dietrich <sdietrich@suse.de>
---
 drivers/media/video/saa7134/saa7134-core.c |    2 +-
 drivers/media/video/zoran_driver.c         |    6 +++---
 2 files changed, 4 insertions(+), 4 deletions(-)

Index: linux-2.6.24-rc2-rt1/drivers/media/video/zoran_driver.c
===================================================================
--- linux-2.6.24-rc2-rt1.orig/drivers/media/video/zoran_driver.c
+++ linux-2.6.24-rc2-rt1/drivers/media/video/zoran_driver.c
@@ -1174,7 +1174,7 @@ zoran_close_end_session (struct file *fi
 
 	/* v4l capture */
 	if (fh->v4l_buffers.active != ZORAN_FREE) {
-		long flags;
+		unsigned long flags;
 
 		spin_lock_irqsave(&zr->spinlock, flags);
 		zr36057_set_memgrab(zr, 0);
@@ -3447,7 +3447,7 @@ zoran_do_ioctl (struct inode *inode,
 
 			/* unload capture */
 			if (zr->v4l_memgrab_active) {
-				long flags;
+				unsigned long flags;
 
 				spin_lock_irqsave(&zr->spinlock, flags);
 				zr36057_set_memgrab(zr, 0);
@@ -4387,7 +4387,7 @@ zoran_vm_close (struct vm_area_struct *v
 				mutex_lock(&zr->resource_lock);
 
 				if (fh->v4l_buffers.active != ZORAN_FREE) {
-					long flags;
+					unsigned long flags;
 
 					spin_lock_irqsave(&zr->spinlock, flags);
 					zr36057_set_memgrab(zr, 0);
Index: linux-2.6.24-rc2-rt1/drivers/media/video/saa7134/saa7134-core.c
===================================================================
--- linux-2.6.24-rc2-rt1.orig/drivers/media/video/saa7134/saa7134-core.c
+++ linux-2.6.24-rc2-rt1/drivers/media/video/saa7134/saa7134-core.c
@@ -1203,7 +1203,7 @@ static int saa7134_resume(struct pci_dev
 {
 
 	struct saa7134_dev *dev = pci_get_drvdata(pci_dev);
-	unsigned int flags;
+	unsigned long flags;
 
 	pci_restore_state(pci_dev);
 	pci_set_power_state(pci_dev, PCI_D0);
