---
 drivers/net/8139too.c |    6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

Index: linux-2.6.22/drivers/net/8139too.c
===================================================================
--- linux-2.6.22.orig/drivers/net/8139too.c	2007-07-24 08:56:39.000000000 +0200
+++ linux-2.6.22/drivers/net/8139too.c	2007-07-24 08:57:01.000000000 +0200
@@ -2214,7 +2214,11 @@ static irqreturn_t rtl8139_interrupt (in
  */
 static void rtl8139_poll_controller(struct net_device *dev)
 {
-	disable_irq(dev->irq);
+	/*
+	 * use _nosync() variant - might be used by netconsole
+	 * from atomic contexts:
+	 */
+	disable_irq_nosync(dev->irq);
 	rtl8139_interrupt(dev->irq, dev);
 	enable_irq(dev->irq);
 }
