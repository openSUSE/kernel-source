---
 include/linux/spinlock.h |    7 +++++++
 1 file changed, 7 insertions(+)

Index: linux-2.6.22/include/linux/spinlock.h
===================================================================
--- linux-2.6.22.orig/include/linux/spinlock.h	2007-07-24 08:56:38.000000000 +0200
+++ linux-2.6.22/include/linux/spinlock.h	2007-07-24 08:57:03.000000000 +0200
@@ -282,6 +282,13 @@ do {						\
 	1 : ({ local_irq_restore(flags); 0; }); \
 })
 
+#define write_trylock_irqsave(lock, flags) \
+({ \
+	local_irq_save(flags);				\
+	write_trylock(lock) ?				\
+		1 : ({ local_irq_restore(flags); 0; });	\
+})
+
 /*
  * Locks two spinlocks l1 and l2.
  * l1_first indicates if spinlock l1 should be taken first.
