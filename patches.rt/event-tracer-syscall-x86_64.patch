Subject: Linux-RT 2.6.27-RT
From: http://www.kernel.org/pub/linux/kernel/projects/rt/
Acked-by: Tony Jones <tonyj@suse.de>
Add hooks to x86 to track syscalls for event trace.

This code was taken from the work by Ingo Molnar.

Signed-off-by: Steven Rostedt <srostedt@redhat.com>
---
 arch/x86/ia32/ia32entry.S   |    9 +++++++
 arch/x86/kernel/entry_64.S  |    4 +++
 include/asm-x86/calling.h   |   50 ++++++++++++++++++++++++++++++++++++++++++++
 include/asm-x86/unistd_64.h |    2 +
 4 files changed, 64 insertions(+), 1 deletion(-)

Index: linux-2.6.27-RT/arch/x86/ia32/ia32entry.S
===================================================================
--- linux-2.6.27-RT.orig/arch/x86/ia32/ia32entry.S
+++ linux-2.6.27-RT/arch/x86/ia32/ia32entry.S
@@ -319,7 +319,9 @@ cstar_do_call:
 	ja  ia32_badsys
 	IA32_ARG_FIXUP 1
 cstar_dispatch:
+	TRACE_SYS_IA32_CALL
 	call *ia32_sys_call_table(,%rax,8)
+	TRACE_SYS_RET
 	movq %rax,RAX-ARGOFFSET(%rsp)
 	GET_THREAD_INFO(%r10)
 	DISABLE_INTERRUPTS(CLBR_NONE)
@@ -428,8 +430,12 @@ ia32_do_syscall:
 	cmpl $(IA32_NR_syscalls-1),%eax
 	ja  int_ret_from_sys_call	/* ia32_tracesys has set RAX(%rsp) */
 	IA32_ARG_FIXUP
+	TRACE_SYS_IA32_CALL
+	TRACE_SYS_IA32_CALL
 	call *ia32_sys_call_table(,%rax,8) # xxx: rip relative
 ia32_sysret:
+	TRACE_SYS_RET
+	TRACE_SYS_RET
 	movq %rax,RAX-ARGOFFSET(%rsp)
 	jmp int_ret_from_sys_call 
 
@@ -498,7 +504,7 @@ END(ia32_ptregs_common)
 
 	.section .rodata,"a"
 	.align 8
-ia32_sys_call_table:
+ENTRY(ia32_sys_call_table)
 	.quad sys_restart_syscall
 	.quad sys_exit
 	.quad stub32_fork
@@ -832,4 +838,5 @@ ia32_sys_call_table:
 	.quad sys_pfm_getinfo_evtsets
 	.quad sys_pfm_delete_evtsets
 	.quad sys_pfm_unload_context
+.globl ia32_syscall_end
 ia32_syscall_end:
Index: linux-2.6.27-RT/arch/x86/kernel/entry_64.S
===================================================================
--- linux-2.6.27-RT.orig/arch/x86/kernel/entry_64.S
+++ linux-2.6.27-RT/arch/x86/kernel/entry_64.S
@@ -363,7 +363,9 @@ system_call_fastpath:
 	cmpq $__NR_syscall_max,%rax
 	ja badsys
 	movq %r10,%rcx
+	TRACE_SYS_CALL
 	call *sys_call_table(,%rax,8)  # XXX:	 rip relative
+	TRACE_SYS_RET
 	movq %rax,RAX-ARGOFFSET(%rsp)
 /*
  * Syscall return path ending with SYSRET (fast path)
@@ -485,7 +487,9 @@ tracesys:
 	cmpq $__NR_syscall_max,%rax
 	ja   int_ret_from_sys_call	/* RAX(%rsp) set to -ENOSYS above */
 	movq %r10,%rcx	/* fixup for C */
+	TRACE_SYS_CALL
 	call *sys_call_table(,%rax,8)
+ 	TRACE_SYS_RET
 	movq %rax,RAX-ARGOFFSET(%rsp)
 	/* Use IRET because user could have changed frame */
 		
Index: linux-2.6.27-RT/include/asm-x86/calling.h
===================================================================
--- linux-2.6.27-RT.orig/include/asm-x86/calling.h
+++ linux-2.6.27-RT/include/asm-x86/calling.h
@@ -165,6 +165,56 @@
 	RESTORE_ARGS 0, \addskip
 	.endm
 
+
+/*
+ * latency-tracing helpers:
+ */
+
+	.macro TRACE_SYS_CALL
+
+#ifdef CONFIG_EVENT_TRACER
+	SAVE_ARGS
+
+	mov     %rdx, %rcx
+	mov     %rsi, %rdx
+	mov     %rdi, %rsi
+	mov     %rax, %rdi
+
+	call sys_call
+
+	RESTORE_ARGS
+#endif
+	.endm
+
+
+	.macro TRACE_SYS_IA32_CALL
+
+#ifdef CONFIG_EVENT_TRACER
+	SAVE_ARGS
+
+	mov     %rdx, %rcx
+	mov     %rsi, %rdx
+	mov     %rdi, %rsi
+	mov     %rax, %rdi
+
+	call sys_ia32_call
+
+	RESTORE_ARGS
+#endif
+	.endm
+
+	.macro TRACE_SYS_RET
+
+#ifdef CONFIG_EVENT_TRACER
+	SAVE_ARGS
+
+	mov     %rax, %rdi
+
+	call sys_ret
+
+	RESTORE_ARGS
+#endif
+	.endm
 	.macro icebp
 	.byte 0xf1
 	.endm
Index: linux-2.6.27-RT/include/asm-x86/unistd_64.h
===================================================================
--- linux-2.6.27-RT.orig/include/asm-x86/unistd_64.h
+++ linux-2.6.27-RT/include/asm-x86/unistd_64.h
@@ -11,6 +11,8 @@
  * Note: holes are not allowed.
  */
 
+#define NR_syscalls (__NR_syscall_max+1)
+
 /* at least 8 syscall per cacheline */
 #define __NR_read				0
 __SYSCALL(__NR_read, sys_read)
