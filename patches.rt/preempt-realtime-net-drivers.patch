---
 drivers/net/tulip/tulip_core.c |    1 +
 1 file changed, 1 insertion(+)

Index: linux-2.6.22/drivers/net/tulip/tulip_core.c
===================================================================
--- linux-2.6.22.orig/drivers/net/tulip/tulip_core.c	2007-07-24 08:56:11.000000000 +0200
+++ linux-2.6.22/drivers/net/tulip/tulip_core.c	2007-07-24 08:57:32.000000000 +0200
@@ -1803,6 +1803,7 @@ static void __devexit tulip_remove_one (
 	pci_iounmap(pdev, tp->base_addr);
 	free_netdev (dev);
 	pci_release_regions (pdev);
+	pci_disable_device (pdev);
 	pci_set_drvdata (pdev, NULL);
 
 	/* pci_power_off (pdev, -1); */
