---
 arch/x86_64/kernel/io_apic.c |    3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

Index: linux-2.6.22/arch/x86_64/kernel/io_apic.c
===================================================================
--- linux-2.6.22.orig/arch/x86_64/kernel/io_apic.c	2007-07-24 08:56:38.000000000 +0200
+++ linux-2.6.22/arch/x86_64/kernel/io_apic.c	2007-07-24 08:57:03.000000000 +0200
@@ -1405,7 +1405,8 @@ static void ack_apic_level(unsigned int 
 	irq_complete_move(irq);
 #if defined(CONFIG_GENERIC_PENDING_IRQ) || defined(CONFIG_IRQBALANCE)
 	/* If we are moving the irq we need to mask it */
-	if (unlikely(irq_desc[irq].status & IRQ_MOVE_PENDING)) {
+	if (unlikely(irq_desc[irq].status & IRQ_MOVE_PENDING) &&
+	    !(irq_desc[irq].status & IRQ_INPROGRESS)) {
 		do_unmask_irq = 1;
 		mask_IO_APIC_irq(irq);
 	}
